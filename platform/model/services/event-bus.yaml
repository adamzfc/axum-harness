name: event-bus
kind: infrastructure-facade
criticality: high
tenant_scope: global
logical_dependencies:
  - packages/messaging
status: deprecated
description: >
  Transitional placeholder kept only to document that event-bus behavior belongs
  in shared runtime/packages and relay workers, not as a long-term business service.
