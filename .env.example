# ── Google OAuth ────────────────────────────────────────────
# Create at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ── Backend API ─────────────────────────────────────────────
API_URL=http://localhost:3001

# ── Logging ─────────────────────────────────────────────────
# Levels: error, warn, info, debug, trace
# Format: <default_level>,<crate>=<level>,...
RUST_LOG=info,native_tauri=debug

# ── Build Cache (sccache) ──────────────────────────────────
# Install: cargo install sccache
# NOTE: sccache currently fails to compile on Rust 1.94 + Windows
# due to windows-sys API changes. Will be fixed in a future release.
# Once resolved, set RUSTC_WRAPPER=sccache to enable compilation caching.
RUSTC_WRAPPER=

# ── Cache Locations (all on D drive to save C disk space) ──
# CARGO_HOME (registry, git db):  D:\dev-storage\scoop\persist\rustup\.cargo
# RUSTUP_HOME (toolchains):       D:\dev-storage\scoop\persist\rustup\.rustup
# Project target (build output):  D:\dev-storage\cache\cargo-target\{project}
#   (configured per-project in .cargo/config.toml via build.target-dir)

# ── Axum Server (figment, APP_ prefix) ─────────────────────
# These are read by servers/api via figment Env::prefixed("APP_")
# Matching config struct: servers/api/src/config.rs
APP_SERVER_HOST=0.0.0.0
APP_SERVER_PORT=3001
APP_SERVER_REQUEST_TIMEOUT_SECS=30

APP_DATABASE_URL=memory
APP_DATABASE_NS=app
APP_DATABASE_DB=main

APP_CACHE_MAX_CAPACITY=10000
APP_CACHE_TTL_SECS=300

APP_AUTH_JWT_SECRET=dev-secret-change-in-production
APP_AUTH_JWT_EXPIRATION_SECS=86400
