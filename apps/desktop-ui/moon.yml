tasks:
  dev:
    command: 'pnpm dev'
    local: true
    platform: 'system'

  build:
    command: 'pnpm build'
    inputs:
      - 'src/**/*'
      - 'svelte.config.js'
      - 'vite.config.ts'
      - 'package.json'
    platform: 'system'

  check:
    command: 'pnpm check'
    inputs:
      - 'src/**/*'
      - 'tsconfig.json'
    platform: 'system'

  lint:
    command: 'pnpm lint'
    inputs:
      - 'src/**/*'
    platform: 'system'

  format:
    command: 'pnpm format'
    inputs:
      - 'src/**/*'
    platform: 'system'

  # Tauri 相关
  tauri:build:
    command: 'cargo build'
    args:
      - '--manifest-path'
      - 'src-tauri/Cargo.toml'
    inputs:
      - 'src-tauri/**/*'
      - 'src-tauri/Cargo.toml'
    platform: 'system'
