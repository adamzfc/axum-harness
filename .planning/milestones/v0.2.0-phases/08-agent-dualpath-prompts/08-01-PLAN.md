---
phase: 08-agent-dualpath-prompts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/web/app/src/lib/ipc/agent.ts
  - packages/adapters/hosts/tauri/src/commands/agent.rs
  - packages/adapters/hosts/tauri/src/commands/mod.rs
  - apps/client/native/src-tauri/src/lib.rs
  - apps/client/native/src-tauri/Cargo.toml
  - apps/client/web/app/src/routes/(app)/agent/+page.svelte
autonomous: true
requirements:
  - AGENT-01

must_haves:
  truths:
    - "Agent 页面在 Tauri 环境走 IPC invoke + Channel streaming，浏览器环境走 HTTP SSE"
    - "Rust 端 agent_chat command 接收 channel 参数并逐 token 推送"
    - "async-openai crate 已添加到 native-tauri 依赖"
  artifacts:
    - path: "apps/client/web/app/src/lib/ipc/agent.ts"
      provides: "双路径 agent client 封装"
      exports: ["agentChatStream"]
    - path: "packages/adapters/hosts/tauri/src/commands/agent.rs"
      provides: "Tauri agent commands"
      exports: ["agent_chat"]
    - path: "apps/client/web/app/src/routes/(app)/agent/+page.svelte"
      provides: "改造后的 agent 页面，双路径路由"
      contains: "window.__TAURI__"
  key_links:
    - from: "apps/client/web/app/src/lib/ipc/agent.ts"
      to: "packages/adapters/hosts/tauri/src/commands/agent.rs"
      via: "invoke('agent_chat', { channel })"
      pattern: "invoke\\(['\\\"]agent_chat"
    - from: "apps/client/web/app/src/routes/(app)/agent/+page.svelte"
      to: "apps/client/web/app/src/lib/ipc/agent.ts"
      via: "import from $lib/ipc/agent"
      pattern: "from '\\$lib/ipc/agent'"
---

<objective>
实现 Agent 页面 Tauri IPC 双路径：新建 `lib/ipc/agent.ts` 封装双路径 client，新增 Rust `agent_chat` Tauri command 使用 Channel API streaming，改造 `agent/+page.svelte` 运行时检测环境并路由到对应路径。

Purpose: 让 Agent 页面在 Tauri 桌面模式下直接通过 IPC 调用本地 LLM（无需外部 API server），同时在浏览器模式下保持现有 HTTP SSE 行为不变。
Output: 双路径 agent client、Tauri agent command、改造后的 agent 页面
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agent-dualpath-prompts/08-CONTEXT.md

# 关键接口 — 从代码库提取

From `packages/features/agent/src/lib.rs`:
```rust
pub trait AgentService: Send + Sync {
    async fn chat_stream(
        &self,
        conversation_id: &str,
        content: &str,
        api_key: &str,
        base_url: &str,
        model: &str,
    ) -> Result<Pin<Box<dyn Stream<Item = Result<String, AgentError>> + Send>>, AgentError>;
}
```

From `apps/client/web/app/src/lib/ipc/auth.ts`:
```typescript
import { invoke } from '@tauri-apps/api/core';
// invoke 模式参考
```

From `packages/adapters/hosts/tauri/src/commands/counter.rs`:
```rust
#[tauri::command]
pub async fn counter_increment(app: tauri::AppHandle) -> Result<i64, String> {
    let db = app.state::<EmbeddedLibSql>().inner().clone();
    let service = usecases::counter_service::LibSqlCounterService::new(db);
    service.increment().await.map_err(|e| e.to_string())
}
```

From `apps/client/native/src-tauri/src/lib.rs` (handler registration):
```rust
.invoke_handler(tauri::generate_handler![
    auth::start_oauth,
    // ... 其他 handlers
]);
```

From `apps/client/web/app/src/routes/(app)/agent/+page.svelte` (现有 sendMessage):
```typescript
// 现有 HTTP SSE 实现 — 第 129-172 行
// 需要保留为浏览器路径，新增 Tauri 路径
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 lib/ipc/agent.ts 双路径 client 封装</name>
  <files>apps/client/web/app/src/lib/ipc/agent.ts</files>
  <read_first>
    - apps/client/web/app/src/lib/ipc/auth.ts — invoke 模式参考
    - apps/client/web/app/src/routes/(app)/agent/+page.svelte — 现有 sendMessage 逻辑（第 104-173 行），了解 SSE 解析格式
  </read_first>
  <action>
    创建 `apps/client/web/app/src/lib/ipc/agent.ts`，导出 `agentChatStream` 函数。

    函数签名：
    ```typescript
    export async function* agentChatStream(params: {
      conversationId: string;
      content: string;
      apiKey: string;
      baseUrl: string;
      model: string;
    }): AsyncGenerator<string, void, unknown>
    ```

    实现逻辑（per D-04）：
    1. 运行时检测 `(window as { __TAURI__?: unknown }).__TAURI__` 存在性
    2. **Tauri 路径**：使用 `@tauri-apps/api/core` 的 `invoke` + Channel API
       ```typescript
       import { invoke } from '@tauri-apps/api/core';
       import { Channel } from '@tauri-apps/api/core';
       const channel = new Channel<string>();
       channel.onmessage = (chunk) => { /* yield chunk */ };
       await invoke('agent_chat', {
         conversationId: params.conversationId,
         content: params.content,
         apiKey: params.apiKey,
         baseUrl: params.baseUrl,
         model: params.model,
         channel
       });
       ```
       使用 AsyncGenerator 模式，channel.onmessage 回调中调用 generator 的内部 resolve 来 yield token。
    3. **浏览器路径**：复用现有 `fetch()` + SSE 逻辑（与 agent/+page.svelte 第 131-159 行相同）
       - POST 到 `${API_BASE}/api/agent/chat`
       - 解析 `data: ` 前缀的 SSE 行
       - 跳过 `[DONE]` 和 `Error:` 行
       - yield content token

    错误处理：try/catch 包裹，错误时 yield `Error: ${message}` 格式字符串。
  </action>
  <verify>
    <automated>grep -q "agentChatStream" apps/client/web/app/src/lib/ipc/agent.ts && grep -q "__TAURI__" apps/client/web/app/src/lib/ipc/agent.ts && grep -q "Channel" apps/client/web/app/src/lib/ipc/agent.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>agent.ts 文件存在，导出 agentChatStream 函数，包含 __TAURI__ 检测和 Channel 导入，同时包含 fetch SSE fallback 逻辑</done>
</task>

<task type="auto">
  <name>Task 2: 新增 Rust agent_chat Tauri command（Channel streaming）</name>
  <files>
    packages/adapters/hosts/tauri/src/commands/agent.rs
    packages/adapters/hosts/tauri/src/commands/mod.rs
    apps/client/native/src-tauri/Cargo.toml
    apps/client/native/src-tauri/src/lib.rs
  </files>
  <read_first>
    - packages/adapters/hosts/tauri/src/commands/counter.rs — Tauri command 模式参考
    - packages/adapters/hosts/tauri/src/commands/mod.rs — 需添加 pub mod agent
    - packages/features/agent/src/lib.rs — AgentService trait 定义
    - packages/core/usecases/src/agent_service.rs — LibSqlAgentService chat_stream 实现，了解 Stream 返回格式
    - apps/client/native/src-tauri/src/lib.rs — 需注册 agent_chat handler
    - apps/client/native/src-tauri/Cargo.toml — 需添加 async-openai 依赖
  </read_first>
  <action>
    分 4 步完成：

    **步骤 A — 添加 async-openai 依赖到 Cargo.toml：**
    在 `apps/client/native/src-tauri/Cargo.toml` 的 `[dependencies]` 中添加：
    ```toml
    async-openai = "0.27"
    feature_agent = { workspace = true }
    usecases = { workspace = true }
    contracts_api = { workspace = true }
    ```

    **步骤 B — 创建 agent.rs command 文件：**
    创建 `packages/adapters/hosts/tauri/src/commands/agent.rs`，实现 `agent_chat` command：

    ```rust
    use tauri::{AppHandle, Manager, ipc::Channel};
    use feature_agent::AgentService;
    use storage_libsql::EmbeddedLibSql;
    use futures_util::StreamExt;

    #[tauri::command]
    pub async fn agent_chat(
        app: AppHandle,
        conversation_id: String,
        content: String,
        api_key: String,
        base_url: String,
        model: String,
        channel: Channel<String>,
    ) -> Result<(), String> {
        let db = app.state::<EmbeddedLibSql>().inner().clone();
        let http_client = reqwest::Client::new();
        let service = usecases::agent_service::LibSqlAgentService::new(db, http_client);

        let mut stream = service
            .chat_stream(&conversation_id, &content, &api_key, &base_url, &model)
            .await
            .map_err(|e| e.to_string())?;

        while let Some(chunk) = stream.next().await {
            match chunk {
                Ok(text) => {
                    channel.send(&text).map_err(|e| e.to_string())?;
                }
                Err(e) => {
                    channel.send(&format!("Error: {}", e)).map_err(|e| e.to_string())?;
                }
            }
        }

        Ok(())
    }
    ```

    **步骤 C — 注册 agent 模块：**
    在 `packages/adapters/hosts/tauri/src/commands/mod.rs` 添加：
    ```rust
    pub mod agent;
    ```

    **步骤 D — 注册 handler：**
    在 `apps/client/native/src-tauri/src/lib.rs` 的 `invoke_handler` 中添加：
    ```rust
    agent::agent_chat,
    ```
    同时在顶部 import 中添加 `agent` 到 `runtime_tauri::commands` 的 use 语句。

    注意：per D-03 使用 async-openai crate，但现有 `LibSqlAgentService` 已使用 reqwest HTTP client 直接调用 OpenAI API（streaming 模式），所以 Tauri command 层可以直接复用 LibSqlAgentService，无需额外引入 async-openai。async-openai 作为备选方案保留在 Cargo.toml 中供后续使用。
  </action>
  <verify>
    <automated>grep -q "pub mod agent" packages/adapters/hosts/tauri/src/commands/mod.rs && grep -q "agent_chat" packages/adapters/hosts/tauri/src/commands/agent.rs && grep -q "agent::agent_chat" apps/client/native/src-tauri/src/lib.rs && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>agent.rs 存在且包含 agent_chat command，mod.rs 导出 agent 模块，lib.rs 注册 agent_chat handler，Cargo.toml 包含必要依赖</done>
</task>

<task type="auto">
  <name>Task 3: 改造 agent/+page.svelte 双路径路由</name>
  <files>apps/client/web/app/src/routes/(app)/agent/+page.svelte</files>
  <read_first>
    - apps/client/web/app/src/routes/(app)/agent/+page.svelte — 完整文件，了解现有 sendMessage 和 UI 结构
    - apps/client/web/app/src/lib/ipc/agent.ts — 刚创建的双路径 client
    - apps/client/web/app/src/lib/ipc/auth.ts — 参考 Tauri 检测模式
  </read_first>
  <action>
    修改 `apps/client/web/app/src/routes/(app)/agent/+page.svelte` 的 `sendMessage` 函数（第 104-173 行）：

    1. 在 script 顶部添加导入：
       ```typescript
       import { agentChatStream } from '$lib/ipc/agent';
       ```

    2. 改造 `sendMessage` 函数，在 try 块内添加环境检测：
       ```typescript
       const isTauri = typeof window !== 'undefined' && !!(window as { __TAURI__?: unknown }).__TAURI__;
       ```

    3. **Tauri 路径**（isTauri === true）：
       - 调用 `agentChatStream({ conversationId, content, apiKey, baseUrl, model })`
       - 使用 `for await (const chunk of stream)` 循环接收 token
       - 每个 chunk 调用 `appendAssistantChunk(chunk)`
       - 不再使用 fetch + ReadableStream

    4. **浏览器路径**（isTauri === false）：
       - 保持现有 fetch + SSE 逻辑不变（第 131-159 行）
       - 可改为调用 `agentChatStream`（其内部已包含浏览器路径）

    5. 保留所有现有 UI 模板不变（第 180-233 行），只修改 script 部分的 sendMessage 逻辑。

    6. 保留 `getRuntimeApiBase()` 和 `loadSettings()` 函数不变。
  </action>
  <verify>
    <automated>grep -q "agentChatStream" apps/client/web/app/src/routes/\(app\)/agent/+page.svelte && grep -q "__TAURI__" apps/client/web/app/src/routes/\(app\)/agent/+page.svelte && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>+page.svelte 导入 agentChatStream，sendMessage 函数包含 __TAURI__ 检测并路由到 agentChatStream，现有 UI 模板不变</done>
</task>

</tasks>

<verification>
- `cargo check -p native-tauri` 编译通过（无 error）
- `npx tsc --noEmit` 前端类型检查通过
- agent.ts 同时包含 Channel 导入和 fetch SSE fallback
- agent.rs 包含 `#[tauri::command]` 和 `channel.send()`
- +page.svelte 包含 `__TAURI__` 检测和 `agentChatStream` 调用
</verification>

<success_criteria>
- [ ] lib/ipc/agent.ts 创建并导出 agentChatStream，包含 Tauri Channel 和 HTTP SSE 双路径
- [ ] commands/agent.rs 创建，agent_chat command 接收 Channel 参数并通过 channel.send() 推送 token
- [ ] commands/mod.rs 添加 pub mod agent
- [ ] lib.rs 注册 agent::agent_chat 到 invoke_handler
- [ ] Cargo.toml 添加 feature_agent、usecases、contracts_api 依赖
- [ ] agent/+page.svelte 改造 sendMessage，运行时检测 __TAURI__ 并路由到 agentChatStream
- [ ] 现有浏览器 SSE 路径保持不变（通过 agentChatStream 内部 fallback）
</success_criteria>

<output>
After completion, create `.planning/phases/08-agent-dualpath-prompts/08-01-SUMMARY.md`
</output>
