---
phase: 08-agent-dualpath-prompts
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
files_modified: []
autonomous: false
requirements:
  - AGENT-01
  - AGENT-DEV-01

must_haves:
  truths:
    - "Desktop Mode 可以完成完整对话：创建对话 → 发送消息 → 接收 streaming 回复 → 逐 token 显示"
    - "E2E 验证链路：Tauri invoke → Rust command → LibSqlAgentService → OpenAI API → Channel streaming → 前端显示"
    - "验证结果有明确记录（截图 + 日志 + 通过/失败状态）"
  artifacts:
    - path: ".planning/phases/08-agent-dualpath-prompts/08-03-E2E-REPORT.md"
      provides: "E2E 验证报告"
      contains: "## Result"
  key_links:
    - from: "agent/+page.svelte"
      to: "commands/agent.rs"
      via: "invoke('agent_chat', { channel })"
      pattern: "invoke.*agent_chat"
    - from: "commands/agent.rs"
      to: "LibSqlAgentService::chat_stream"
      via: "service.chat_stream() → channel.send()"
      pattern: "channel\\.send"
---

<objective>
验证 Desktop Mode E2E 完整对话流程：在 Tauri 桌面模式下完成从创建对话到 streaming 回复的完整链路，并记录验证报告。

Purpose: 确认 08-01-PLAN.md 实现的双路径 IPC 在真实 Tauri 环境中工作，streaming token 逐字显示正常。
Output: E2E 验证报告（含截图、日志、通过/失败判定）
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agent-dualpath-prompts/08-CONTEXT.md

# E2E 验证链路（per D-09, D-10）

完整链路：
1. Tauri invoke('agent_chat', { channel }) — 前端发起
2. Rust agent_chat command 接收 — 后端处理
3. LibSqlAgentService::chat_stream() — 调用 OpenAI API
4. OpenAI API streaming response — 外部服务
5. channel.send(token) 逐 token 推送 — Channel streaming
6. 前端 channel.onmessage 接收并逐 token 显示 — UI 更新

# 前置条件

- 08-01-PLAN.md 已执行完成（双路径 IPC 实现）
- 需要真实 OpenAI API key（从 Tauri store 读取，agent 页面 loadSettings 已有逻辑）
- Tauri dev server 运行中（`moon run native-tauri:dev` 或等效命令）
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Desktop Mode E2E 完整对话验证</name>
  <what-built>
    Agent 页面双路径 IPC 已实现：
    - lib/ipc/agent.ts 封装 Tauri Channel + HTTP SSE 双路径
    - commands/agent.rs 实现 agent_chat Tauri command
    - agent/+page.svelte 运行时检测环境并路由到对应路径
  </what-built>
  <how-to-verify>
    按以下步骤验证 Desktop Mode 完整对话：

    1. **启动 Tauri 应用**：
       ```bash
       cd apps/client/native && cargo tauri dev
       ```
       或使用 moon：`moon run native-tauri:dev`

    2. **确认 Agent 页面加载**：
       - 导航到 Agent 页面
       - 确认 sidebar 显示 conversation list
       - 确认底部有消息输入框和 Send 按钮

    3. **创建新对话**：
       - 点击 "New Chat" 按钮
       - 确认 sidebar 出现新对话条目
       - 确认主区域显示 "Select or create a conversation" 或空白输入区

    4. **发送测试消息**：
       - 在输入框中输入：`Hello, this is a test of the desktop mode agent chat.`
       - 点击 Send 按钮
       - 观察：
         - 用户消息立即显示在消息区域（右侧，白色气泡）
         - 出现 loading 状态（streaming = true）
         - Assistant 消息区域开始逐 token 显示回复内容
         - **关键验证点**：回复是逐字/逐词出现（streaming），而非一次性显示

    5. **验证 streaming 行为**：
         - 回复过程中，文字应该逐步出现（不是整段突然出现）
         - 如果出现 `[tool:xxx]` 格式文本，说明 tool calling 也在工作
         - 回复完成后，streaming 状态应结束

    6. **截图记录**：
         - 截取完整对话界面（包含 sidebar + 消息区域 + 输入框）
         - 截图应显示至少一轮完整对话（用户消息 + assistant 回复）

    7. **检查控制台日志**：
         - 打开 DevTools Console
         - 确认无 error 级别日志
         - 如有 warning，记录但不阻塞验证
  </how-to-verify>
  <resume-signal>
    截图并报告以下信息：
    1. 对话是否成功完成（是/否）
    2. streaming 是否逐 token 显示（是/否）
    3. 是否有控制台 error（有/无，如有请描述）
    4. 附上截图
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: 生成 E2E 验证报告</name>
  <files>.planning/phases/08-agent-dualpath-prompts/08-03-E2E-REPORT.md</files>
  <read_first>
    - .planning/phases/08-agent-dualpath-prompts/08-CONTEXT.md — Phase 8 目标和决策
  </read_first>
  <action>
    根据 Task 1 的验证结果，创建 `.planning/phases/08-agent-dualpath-prompts/08-03-E2E-REPORT.md`：

    ```markdown
    # Phase 8 E2E Report: Desktop Mode Agent Chat

    **Date:** [执行日期]
    **Phase:** 08-agent-dualpath-prompts
    **Test:** Desktop Mode 完整对话验证

    ## Test Environment
    - OS: [macOS/Windows/Linux]
    - Tauri version: 2.x
    - Browser engine: [WebKit/Chromium]

    ## Test Steps & Results

    | Step | Expected | Actual | Status |
    |------|----------|--------|--------|
    | 1. 启动 Tauri 应用 | 应用正常启动 | [结果] | ☐ PASS / ☐ FAIL |
    | 2. Agent 页面加载 | 显示 sidebar + 输入区 | [结果] | ☐ PASS / ☐ FAIL |
    | 3. 创建新对话 | 新对话出现在 sidebar | [结果] | ☐ PASS / ☐ FAIL |
    | 4. 发送消息 | 用户消息显示，streaming 开始 | [结果] | ☐ PASS / ☐ FAIL |
    | 5. Streaming 回复 | 逐 token 显示 | [结果] | ☐ PASS / ☐ FAIL |
    | 6. 完成对话 | streaming 结束，消息持久化 | [结果] | ☐ PASS / ☐ FAIL |

    ## Screenshot
    [嵌入或引用截图路径]

    ## Console Logs
    [记录任何 error/warning 日志]

    ## IPC Path Verification
    - [ ] 确认使用 Tauri 路径（非 HTTP SSE）：检查 Network tab 无 /api/agent/chat 请求
    - [ ] 确认 invoke('agent_chat') 被调用：DevTools Console 或 Tauri logs

    ## Result
    **Overall:** ☐ PASSED / ☐ FAILED

    [如果 FAILED，描述失败原因和修复建议]

    ## Notes
    [其他观察或备注]
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/08-agent-dualpath-prompts/08-03-E2E-REPORT.md && grep -q "## Result" .planning/phases/08-agent-dualpath-prompts/08-03-E2E-REPORT.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>E2E 报告文件存在，包含 Test Steps & Results 表格、Overall 判定、截图引用、IPC 路径验证</done>
</task>

</tasks>

<verification>
- E2E 验证完成（Task 1 human checkpoint）
- E2E 报告文件存在且包含完整的验证结果
- 确认 Desktop Mode 使用 Tauri IPC 路径（非 HTTP SSE）
- streaming 逐 token 显示行为已验证
</verification>

<success_criteria>
- [ ] Tauri 应用成功启动并加载 Agent 页面
- [ ] 可以创建新对话并出现在 sidebar
- [ ] 发送消息后，assistant 回复通过 Tauri IPC Channel streaming 返回（非 HTTP SSE）
- [ ] streaming 逐 token 显示行为正常
- [ ] 无控制台 error
- [ ] E2E 报告文件生成，包含完整验证步骤和结果
</success_criteria>

<output>
After completion, create `.planning/phases/08-agent-dualpath-prompts/08-03-SUMMARY.md`
</output>
