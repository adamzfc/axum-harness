---
phase: 04-minimal-feature-implementation
plan: '03'
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - packages/adapters/hosts/tauri/src/commands/auth.rs
  - packages/adapters/hosts/tauri/src/commands/mod.rs
  - packages/adapters/hosts/tauri/src/commands/counter.rs
  - packages/adapters/hosts/tauri/src/commands/admin.rs
  - packages/adapters/hosts/tauri/Cargo.toml
  - packages/adapters/hosts/tauri/src/lib.rs
  - apps/client/native/src-tauri/src/lib.rs
  - apps/client/native/src-tauri/Cargo.toml
  - servers/api/src/routes/mod.rs
  - servers/api/src/routes/counter.rs
  - servers/api/src/routes/admin.rs
  - apps/client/web/app/src/routes/(app)/counter/+page.svelte
  - apps/client/web/app/src/routes/(app)/admin/+page.svelte
  - apps/client/web/app/src/routes/(app)/+layout.svelte
autonomous: true
requirements:
  - AUTH-01
  - COUNTER-01
  - ADMIN-01

must_haves:
  truths:
    - "auth.rs in runtime_tauri imports from adapter-google, re-exports as thin wrapper"
    - "Tauri commands counter_increment, counter_decrement, counter_reset, counter_get_value registered"
    - "Tauri commands admin_get_dashboard_stats registered"
    - "Axum routes for /api/counter/* and /api/admin/stats mounted"
    - "Counter page uses IPC dual-path (invoke in Tauri, fetch in browser)"
    - "Admin page displays real data from AdminService (not mock)"
    - "Navigation includes Agent Chat link"
    - "cargo check --workspace passes"
  artifacts:
    - path: "packages/adapters/hosts/tauri/src/commands/counter.rs"
      provides: "Tauri counter commands (increment, decrement, reset, get_value)"
      contains: "counter_increment"
      contains: "counter_get_value"
    - path: "packages/adapters/hosts/tauri/src/commands/admin.rs"
      provides: "Tauri admin commands (get_dashboard_stats)"
      contains: "admin_get_dashboard_stats"
    - path: "servers/api/src/routes/counter.rs"
      provides: "Axum counter REST endpoints"
      contains: "POST /increment"
      contains: "GET /value"
    - path: "servers/api/src/routes/admin.rs"
      provides: "Axum admin REST endpoints"
      contains: "GET /stats"
    - path: "apps/client/web/app/src/routes/(app)/counter/+page.svelte"
      provides: "Counter page with IPC integration"
      contains: "invoke"
    - path: "apps/client/web/app/src/routes/(app)/admin/+page.svelte"
      provides: "Admin page with real data"
      contains: "DashboardStats"
  key_links:
    - from: "packages/adapters/hosts/tauri/src/commands/counter.rs"
      to: "packages/features/counter/src/lib.rs"
      via: "CounterService trait usage"
      pattern: "CounterService|increment|decrement"
    - from: "servers/api/src/routes/counter.rs"
      to: "packages/features/counter/src/lib.rs"
      via: "CounterService trait usage"
      pattern: "CounterService"
    - from: "apps/client/web/app/src/routes/(app)/counter/+page.svelte"
      to: "Tauri commands + HTTP endpoints"
      via: "IPC dual-path runtime detection"
      pattern: "window\\.__TAURI__|fetch.*api/counter"
    - from: "apps/client/native/src-tauri/src/lib.rs"
      to: "runtime_tauri counter/admin commands"
      via: "generate_handler! registration"
      pattern: "counter_.*|admin_.*"
---

<objective>
Wire counter and admin features end-to-end: Tauri commands, Axum routes, and frontend pages.

Purpose: Connect feature crates to actual runtime. Create Tauri counter/admin commands, Axum REST endpoints, update counter page to use IPC dual-path, update admin page to show real data, add Agent Chat to navigation.
Output: Working counter (increment/decrement/reset/persist) and admin (real stats) in both Tauri and browser environments.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-minimal-feature-implementation/04-CONTEXT.md
@.planning/phases/04-minimal-feature-implementation/04-RESEARCH.md
@packages/adapters/hosts/tauri/src/commands/auth.rs
@packages/adapters/hosts/tauri/src/commands/mod.rs
@packages/adapters/hosts/tauri/src/lib.rs
@packages/adapters/hosts/tauri/Cargo.toml
@apps/client/native/src-tauri/src/lib.rs
@apps/client/native/src-tauri/Cargo.toml
@servers/api/src/routes/mod.rs
@servers/api/src/state.rs
@packages/features/counter/src/lib.rs
@packages/features/admin/src/lib.rs
@packages/core/usecases/src/counter_service.rs
@packages/core/usecases/src/admin_service.rs
@packages/adapters/storage/libsql/src/embedded.rs
@apps/client/web/app/src/routes/(app)/counter/+page.svelte
@apps/client/web/app/src/routes/(app)/admin/+page.svelte
@apps/client/web/app/src/routes/(app)/+layout.svelte

<interfaces>
From packages/features/counter/src/lib.rs:
```rust
pub trait CounterService: Send + Sync {
    async fn get_value(&self) -> Result<i64, CounterError>;
    async fn increment(&self) -> Result<i64, CounterError>;
    async fn decrement(&self) -> Result<i64, CounterError>;
    async fn reset(&self) -> Result<i64, CounterError>;
}
```

From packages/features/admin/src/lib.rs:
```rust
pub struct DashboardStats { pub tenant_count, counter_value: i64, pub last_login: Option<String>, pub app_version: String }
pub trait AdminService: Send + Sync {
    async fn get_dashboard_stats(&self) -> Result<DashboardStats, AdminError>;
}
```

From packages/core/usecases/src/counter_service.rs:
```rust
pub const COUNTER_MIGRATION: &str = "CREATE TABLE IF NOT EXISTS counter ...";
pub struct LibSqlCounterService<P: LibSqlPort> { port: P }
```

From packages/adapters/storage/libsql/src/embedded.rs:
```rust
pub struct EmbeddedLibSql { /* ... */ }
pub async fn run_tenant_migrations(db: &EmbeddedLibSql) -> Result<(), LibSqlError>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Tauri counter and admin commands</name>
  <read_first>packages/adapters/hosts/tauri/src/commands/auth.rs (existing command pattern), packages/adapters/hosts/tauri/Cargo.toml, packages/features/counter/src/lib.rs, packages/features/admin/src/lib.rs</read_first>
  <files>packages/adapters/hosts/tauri/src/commands/counter.rs, packages/adapters/hosts/tauri/src/commands/admin.rs, packages/adapters/hosts/tauri/src/commands/mod.rs, packages/adapters/hosts/tauri/Cargo.toml</files>
  <action>
    1. Add to `packages/adapters/hosts/tauri/Cargo.toml` dependencies:
    ```toml
    feature-counter = { workspace = true }
    feature-admin = { workspace = true }
    usecases = { workspace = true }
    storage_libsql = { workspace = true }
    ```

    2. Create `packages/adapters/hosts/tauri/src/commands/counter.rs`:
    ```rust
    //! Counter Tauri commands — bridge to CounterService.

    use feature_counter::CounterService;
    use storage_libsql::EmbeddedLibSql;
    use tauri::State;
    use crate::state::AppState;

    #[tauri::command]
    pub async fn counter_increment(state: State<'_, AppState>) -> Result<i64, String> {
        let service = usecases::counter_service::LibSqlCounterService::new(state.db.clone());
        service.increment().await.map_err(|e| e.to_string())
    }

    #[tauri::command]
    pub async fn counter_decrement(state: State<'_, AppState>) -> Result<i64, String> {
        let service = usecases::counter_service::LibSqlCounterService::new(state.db.clone());
        service.decrement().await.map_err(|e| e.to_string())
    }

    #[tauri::command]
    pub async fn counter_reset(state: State<'_, AppState>) -> Result<i64, String> {
        let service = usecases::counter_service::LibSqlCounterService::new(state.db.clone());
        service.reset().await.map_err(|e| e.to_string())
    }

    #[tauri::command]
    pub async fn counter_get_value(state: State<'_, AppState>) -> Result<i64, String> {
        let service = usecases::counter_service::LibSqlCounterService::new(state.db.clone());
        service.get_value().await.map_err(|e| e.to_string())
    }
    ```

    3. Create `packages/adapters/hosts/tauri/src/commands/admin.rs`:
    ```rust
    //! Admin Tauri commands — bridge to AdminService.

    use feature_admin::{AdminService, DashboardStats};
    use tauri::State;
    use crate::state::AppState;

    #[tauri::command]
    pub async fn admin_get_dashboard_stats(state: State<'_, AppState>) -> Result<DashboardStats, String> {
        let tenant_service = usecases::tenant_service::LibSqlTenantService::new(state.db.clone());
        let counter_service = usecases::counter_service::LibSqlCounterService::new(state.db.clone());
        let admin_service = usecases::admin_service::LibSqlAdminService::new(state.db.clone(), tenant_service, counter_service);
        admin_service.get_dashboard_stats().await.map_err(|e| e.to_string())
    }
    ```

    4. Update `packages/adapters/hosts/tauri/src/commands/mod.rs`:
    ```rust
    pub mod auth;
    pub mod config;
    pub mod counter;
    pub mod admin;
    ```
  </action>
  <verify>
    <automated>cargo check -p runtime_tauri</automated>
  </verify>
  <done>
    Tauri counter commands (counter_increment, counter_decrement, counter_reset, counter_get_value) and admin command (admin_get_dashboard_stats) compile in runtime_tauri.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register Tauri commands + run counter migrations</name>
  <read_first>apps/client/native/src-tauri/src/lib.rs (current handler + setup), packages/core/usecases/src/counter_service.rs (migration SQL)</read_first>
  <files>apps/client/native/src-tauri/src/lib.rs, apps/client/native/src-tauri/Cargo.toml</files>
  <action>
    1. Add to `apps/client/native/src-tauri/Cargo.toml`:
    ```toml
    feature-counter = { workspace = true }
    usecases = { workspace = true }
    ```

    2. Update `apps/client/native/src-tauri/src/lib.rs`:
       - Add imports: `use runtime_tauri::commands::{auth, config, counter, admin};`
       - Add counter migration to setup after `run_tenant_migrations`:
         ```rust
         db.execute(&usecases::counter_service::COUNTER_MIGRATION, vec![])
             .await
             .expect("Failed to run counter migration");
         ```
       - Add to generate_handler!:
         ```rust
         counter::counter_increment,
         counter::counter_decrement,
         counter::counter_reset,
         counter::counter_get_value,
         admin::admin_get_dashboard_stats,
         ```
  </action>
  <verify>
    <automated>cargo check -p native-tauri</automated>
  </verify>
  <done>
    native-tauri compiles with counter + admin commands registered. Counter table created on app startup.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Axum counter and admin routes</name>
  <read_first>servers/api/src/routes/mod.rs, servers/api/src/routes/tenant.rs (existing route pattern), servers/api/src/state.rs</read_first>
  <files>servers/api/src/routes/counter.rs, servers/api/src/routes/admin.rs, servers/api/src/routes/mod.rs, servers/api/Cargo.toml</files>
  <action>
    1. Add to `servers/api/Cargo.toml`:
    ```toml
    feature-counter = { workspace = true }
    feature-admin = { workspace = true }
    usecases = { workspace = true }
    ```

    2. Create `servers/api/src/routes/counter.rs`:
    ```rust
    //! Counter REST API routes.

    use axum::{Router, routing::{post, get}, Json, extract::State};
    use crate::state::AppState;
    use feature_counter::CounterService;
    use serde::Deserialize;

    pub fn router() -> Router<AppState> {
        Router::new()
            .route("/counter/increment", post(increment))
            .route("/counter/decrement", post(decrement))
            .route("/counter/reset", post(reset))
            .route("/counter/value", get(get_value))
    }

    async fn increment(State(state): State<AppState>) -> Json<serde_json::Value> {
        let service = usecases::counter_service::LibSqlCounterService::new(state.db.clone());
        match service.increment().await {
            Ok(value) => Json(serde_json::json!({ "value": value })),
            Err(e) => Json(serde_json::json!({ "error": e.to_string() })),
        }
    }

    async fn decrement(State(state): State<AppState>) -> Json<serde_json::Value> {
        let service = usecases::counter_service::LibSqlCounterService::new(state.db.clone());
        match service.decrement().await {
            Ok(value) => Json(serde_json::json!({ "value": value })),
            Err(e) => Json(serde_json::json!({ "error": e.to_string() })),
        }
    }

    async fn reset(State(state): State<AppState>) -> Json<serde_json::Value> {
        let service = usecases::counter_service::LibSqlCounterService::new(state.db.clone());
        match service.reset().await {
            Ok(value) => Json(serde_json::json!({ "value": value })),
            Err(e) => Json(serde_json::json!({ "error": e.to_string() })),
        }
    }

    async fn get_value(State(state): State<AppState>) -> Json<serde_json::Value> {
        let service = usecases::counter_service::LibSqlCounterService::new(state.db.clone());
        match service.get_value().await {
            Ok(value) => Json(serde_json::json!({ "value": value })),
            Err(e) => Json(serde_json::json!({ "error": e.to_string() })),
        }
    }
    ```

    3. Create `servers/api/src/routes/admin.rs`:
    ```rust
    //! Admin REST API routes.

    use axum::{Router, routing::get, Json, extract::State};
    use crate::state::AppState;
    use feature_admin::AdminService;

    pub fn router() -> Router<AppState> {
        Router::new()
            .route("/admin/stats", get(get_dashboard_stats))
    }

    async fn get_dashboard_stats(State(state): State<AppState>) -> Json<serde_json::Value> {
        let tenant_service = usecases::tenant_service::LibSqlTenantService::new(state.db.clone());
        let counter_service = usecases::counter_service::LibSqlCounterService::new(state.db.clone());
        let admin_service = usecases::admin_service::LibSqlAdminService::new(state.db.clone(), tenant_service, counter_service);
        match admin_service.get_dashboard_stats().await {
            Ok(stats) => Json(serde_json::json!(stats)),
            Err(e) => Json(serde_json::json!({ "error": e.to_string() })),
        }
    }
    ```

    4. Update `servers/api/src/routes/mod.rs`:
       - Add `pub mod counter;` and `pub mod admin;`
       - Merge into api_router: `.merge(counter::router()).merge(admin::router())`
  </action>
  <verify>
    <automated>cargo check -p api-server</automated>
  </verify>
  <done>
    Axum server compiles with counter and admin routes. Counter routes: POST /counter/increment, POST /counter/decrement, POST /counter/reset, GET /counter/value. Admin route: GET /admin/stats.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update counter and admin frontend pages</name>
  <read_first>apps/client/web/app/src/routes/(app)/counter/+page.svelte, apps/client/web/app/src/routes/(app)/admin/+page.svelte, apps/client/web/app/src/routes/(app)/+layout.svelte</read_first>
  <files>apps/client/web/app/src/routes/(app)/counter/+page.svelte, apps/client/web/app/src/routes/(app)/admin/+page.svelte, apps/client/web/app/src/routes/(app)/+layout.svelte</files>
  <action>
    1. Update counter page — replace local state with IPC dual-path:
    ```svelte
    <script lang="ts">
    import { Button } from '$lib/components';
    import { Plus, Minus, RotateCcw } from '@jis3r/icons';
    import { onMount } from 'svelte';

    let count = $state(0);
    let loading = $state(false);

    const isTauri = typeof window !== 'undefined' && (window as any).__TAURI__;

    async function invokeCommand(cmd: string) {
      if (isTauri) {
        return (window as any).__TAURI__.core.invoke(cmd);
      } else {
        const method = cmd.startsWith('counter_get') ? 'GET' : 'POST';
        const endpoint = cmd.replace('counter_', '').replace('get_value', 'value');
        const url = `http://localhost:3000/api/counter/${endpoint}`;
        const resp = await fetch(url, { method });
        const data = await resp.json();
        return data.value;
      }
    }

    async function loadValue() {
      loading = true;
      try { count = await invokeCommand('counter_get_value'); } catch {}
      loading = false;
    }

    async function increment() { count = await invokeCommand('counter_increment'); }
    async function decrement() { count = await invokeCommand('counter_decrement'); }
    async function reset() { count = await invokeCommand('counter_reset'); }

    onMount(() => { loadValue(); });
    </script>

    <div class="flex min-h-screen flex-col items-center justify-center gap-8 bg-[var(--color-bg)] px-4">
      <div class="font-mono text-8xl sm:text-9xl text-[var(--color-text)] tabular-nums py-8 select-none">
        {count}
      </div>
      <div class="flex flex-row items-center gap-4">
        <Button variant="secondary" size="lg" class="h-12 w-12" onclick={decrement}>
          {#snippet icon()}<Minus class="h-5 w-5" />{/snippet}{''}
        </Button>
        <Button variant="primary" size="lg" class="h-12 w-12" onclick={increment}>
          {#snippet icon()}<Plus class="h-5 w-5" />{/snippet}{''}
        </Button>
      </div>
      <Button variant="ghost" size="md" onclick={reset}>
        {#snippet icon()}<RotateCcw class="h-4 w-4" />{/snippet}Reset
      </Button>
    </div>
    ```

    2. Update admin page — replace mock data with API call:
    ```svelte
    <script lang="ts">
    import { Card, Badge } from '$lib/components';
    import { onMount } from 'svelte';

    let stats = $state({ tenant_count: 0, counter_value: 0, last_login: null as string | null, app_version: '0.0.0' });
    let loading = $state(true);

    const isTauri = typeof window !== 'undefined' && (window as any).__TAURI__;

    async function fetchStats() {
      loading = true;
      try {
        if (isTauri) {
          stats = await (window as any).__TAURI__.core.invoke('admin_get_dashboard_stats');
        } else {
          const resp = await fetch('http://localhost:3000/api/admin/stats');
          stats = await resp.json();
        }
      } catch (e) {
        console.error('Failed to load stats:', e);
      }
      loading = false;
    }

    onMount(() => { fetchStats(); });

    const statCards = $derived([
      { label: 'Tenants', value: String(stats.tenant_count), icon: '🏢' },
      { label: 'Counter', value: String(stats.counter_value), icon: '🔢' },
      { label: 'Last Login', value: stats.last_login ? new Date(stats.last_login).toLocaleDateString() : 'N/A', icon: '👤' },
      { label: 'Version', value: stats.app_version, icon: '📦' },
    ]);
    </script>

    <div class="p-4 md:p-6 space-y-6">
      <div>
        <h1 class="text-2xl font-semibold text-[var(--color-text)]">Admin Dashboard</h1>
        <p class="text-sm text-[var(--color-text-muted)] mt-1">Real-time application metrics</p>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        {#each statCards as stat}
          <Card class="p-5">
            <div class="flex items-center justify-between">
              <p class="text-sm text-[var(--color-text-muted)]">{stat.label}</p>
              <span class="text-lg">{stat.icon}</span>
            </div>
            <p class="text-2xl font-semibold text-[var(--color-text)] mt-2">{loading ? '...' : stat.value}</p>
          </Card>
        {/each}
      </div>
    </div>
    ```

    3. Update layout — add Agent Chat to navItems:
    ```javascript
    const navItems = [
      { href: '/counter', label: 'Counter', icon: Plus },
      { href: '/admin', label: 'Admin', icon: LayoutDashboard },
      { href: '/agent', label: 'Agent Chat', icon: MessageSquare },
      { href: '/settings', label: 'Settings', icon: Settings },
    ];
    ```
    Add `MessageSquare` import from `@jis3r/icons`.
  </action>
  <verify>
    <automated>cd apps/client/web && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
    Counter page uses IPC dual-path (invoke in Tauri, fetch in browser). Admin page shows real tenant count, counter value, last login, and version. Navigation includes Agent Chat link.
  </done>
</task>

</tasks>

<verification>
- `cargo check -p runtime_tauri` passes (counter + admin commands)
- `cargo check -p native-tauri` passes (commands registered, migrations run)
- `cargo check -p api-server` passes (counter + admin routes)
- `cargo check --workspace` passes
- `grep -q "counter_increment" apps/client/native/src-tauri/src/lib.rs`
- `grep -q "admin_get_dashboard_stats" apps/client/native/src-tauri/src/lib.rs`
- `grep -q "Agent Chat" apps/client/web/app/src/routes/(app)/+layout.svelte`
</verification>

<success_criteria>
- Tauri commands for counter (increment, decrement, reset, get_value) registered and functional
- Tauri command for admin (get_dashboard_stats) registered and functional
- Axum REST endpoints for counter and admin mounted
- Counter page uses IPC dual-path (works in both Tauri and browser)
- Admin page shows real data from TenantService + CounterService
- Navigation includes Agent Chat entry
- Counter migration runs on app startup
- cargo check --workspace passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-minimal-feature-implementation/04-03-SUMMARY.md`
</output>
