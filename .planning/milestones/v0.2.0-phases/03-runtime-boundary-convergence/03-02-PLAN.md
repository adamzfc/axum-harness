---
phase: 03-runtime-boundary-convergence
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/usecases/Cargo.toml
autonomous: true
requirements:
  - RUNTIME-01

must_haves:
  truths:
    - "usecases crate does not depend on contracts_api"
    - "usecases only depends on domain (plus utility crates: async-trait, serde, serde_json, chrono, thiserror)"
    - "cargo tree -p usecases --depth 1 shows no contracts_api"
    - "cargo check -p usecases passes"
    - "All usecases tests still pass"
  artifacts:
    - path: "packages/core/usecases/Cargo.toml"
      provides: "usecases crate manifest without contracts_api dependency"
      contains: 'domain = { workspace = true }'
  key_links:
    - from: "packages/core/usecases/Cargo.toml"
      to: "contracts_api"
      via: "dependency removed"
      pattern: "!contracts_api"
---

<objective>
Remove contracts_api dependency from usecases crate to enforce hexagonal boundary: usecases must only depend on domain.

Purpose: usecases currently has `contracts_api = { workspace = true }` in its Cargo.toml, which violates the boundary rule that core layers must not depend on contracts. The actual code in tenant_service.rs doesn't import from contracts_api, so this is a pure Cargo.toml cleanup.
Output: usecases/Cargo.toml with contracts_api line removed. Workspace still compiles.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-runtime-boundary-convergence/03-CONTEXT.md
@.planning/phases/03-runtime-boundary-convergence/03-RESEARCH.md

# Design decisions from context (MANDATORY)
- D-05: usecases crate no longer depends on contracts_api
- D-08: usecases only depends on domain (Port traits)

# Current state of usecases/Cargo.toml:
```toml
[dependencies]
domain = { workspace = true }
contracts_api = { workspace = true }  ← REMOVE THIS
async-trait = "0.1"
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
thiserror = { workspace = true }
```

# Verification that code doesn't use contracts_api
tenant_service.rs imports: domain::ports::lib_sql::LibSqlPort, domain::ports::surreal_db::SurrealDbPort
No file in usecases/src/ imports from contracts_api.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove contracts_api dependency from usecases</name>
  <files>
    - packages/core/usecases/Cargo.toml
  </files>
  <read_first>
    - packages/core/usecases/Cargo.toml
    - packages/core/usecases/src/lib.rs (verify no contracts_api imports)
    - packages/core/usecases/src/tenant_service.rs (verify no contracts_api imports)
  </read_first>
  <action>
Edit `packages/core/usecases/Cargo.toml`:
- REMOVE the line: `contracts_api = { workspace = true }`
- Keep all other dependencies unchanged (domain, async-trait, serde, serde_json, chrono, thiserror)
  </action>
  <acceptance_criteria>
    - packages/core/usecases/Cargo.toml does NOT contain "contracts_api"
    - cargo check -p usecases passes
    - cargo test -p usecases passes
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "contracts_api" packages/core/usecases/Cargo.toml && cargo check -p usecases 2>&1 | tail -3</automated>
  </verify>
  <done>usecases crate has no contracts_api dependency, still compiles and tests pass</done>
</task>

</tasks>

<verification>
- [ ] cargo check -p usecases passes
- [ ] cargo test -p usecases passes
- [ ] grep confirms no contracts_api in usecases/Cargo.toml
</verification>

<success_criteria>
usecases depends only on domain and utility crates. No contracts_api dependency exists. Hexagonal boundary enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-boundary-convergence/03-02-SUMMARY.md`
</output>
