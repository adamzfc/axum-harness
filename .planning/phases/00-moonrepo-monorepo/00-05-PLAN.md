---
phase: 00-moonrepo-monorepo
plan: 05
type: execute
wave: 3
depends_on:
  - 00-01
files_modified:
  - rust-toolchain.toml
  - rustfmt.toml
  - clippy.toml
  - apps/desktop-ui/.prettierrc
  - apps/desktop-ui/.prettierignore
  - apps/desktop-ui/eslint.config.js
  - apps/desktop-ui/.editorconfig
autonomous: true
requirements: []

must_haves:
  truths:
    - "rustfmt 配置可用"
    - "clippy 配置可用"
    - "prettier + eslint 配置可用"
    - "开发者可统一执行格式化和 lint"
  artifacts:
    - path: "rust-toolchain.toml"
      provides: "Rust 工具链锁定"
      contains: "channel"
    - path: "rustfmt.toml"
      provides: "Rust 格式化配置"
    - path: "apps/desktop-ui/.prettierrc"
      provides: "前端格式化配置"
    - path: "apps/desktop-ui/eslint.config.js"
      provides: "前端 lint 配置"
  key_links:
    - from: "rustfmt.toml"
      to: "cargo fmt"
      via: "格式化规则"
    - from: "apps/desktop-ui/.prettierrc"
      to: "pnpm format"
      via: "prettier 配置"
---

<objective>
## Plan 05: 开发工具链配置

**Purpose:** 配置 rustfmt、clippy、prettier、eslint 开发工具链
**Output:** 工具链配置文件，确保代码格式和质量一致
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/03_system_architecture_spec.md

## 工具链需求（来自 ROADMAP Phase 0 scope item 7）

- rustfmt: Rust 代码格式化
- clippy: Rust lint
- eslint/prettier: SvelteKit 前端
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 配置 Rust 工具链</name>
  <files>rust-toolchain.toml, rustfmt.toml, clippy.toml</files>
  <action>
    创建 Rust 工具链配置文件：

    1. `rust-toolchain.toml`（锁定 Rust 版本）:
    ```toml
    [toolchain]
    channel = "1.82.0"
    components = ["rustfmt", "clippy"]
    ```

    2. `rustfmt.toml`（格式化规则）:
    ```toml
    edition = "2021"
    max_width = 100
    tab_spaces = 4
    use_field_init_shorthand = true
    use_try_shorthand = true
    ```

    3. `clippy.toml`（lint 规则）:
    ```toml
    # clippy 配置
    too-many-arguments-threshold = 8
    type-complexity-threshold = 300
    ```
  </action>
  <verify>
    <automated>test -f rust-toolchain.toml && test -f rustfmt.toml && test -f clippy.toml && echo "RUST_TOOLCHAIN_OK"</automated>
  </verify>
  <done>rust-toolchain.toml、rustfmt.toml、clippy.toml 已创建</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 配置前端工具链</name>
  <files>apps/desktop-ui/.prettierrc, apps/desktop-ui/.prettierignore, apps/desktop-ui/eslint.config.js, apps/desktop-ui/.editorconfig</files>
  <action>
    创建前端工具链配置文件：

    1. `apps/desktop-ui/.prettierrc`:
    ```json
    {
      "useTabs": false,
      "tabWidth": 2,
      "semi": true,
      "singleQuote": true,
      "trailingComma": "all",
      "printWidth": 100,
      "plugins": ["prettier-plugin-svelte"],
      "overrides": [
        {
          "files": "*.svelte",
          "options": {
            "parser": "svelte"
          }
        }
      ]
    }
    ```

    2. `apps/desktop-ui/.prettierignore`:
    ```
    .svelte-kit
    build
    node_modules
    ```

    3. `apps/desktop-ui/eslint.config.js`:
    ```javascript
    import js from '@eslint/js';
    import svelte from 'eslint-plugin-svelte';
    import ts from 'typescript-eslint';

    export default ts.config(
      js.configs.recommended,
      ...ts.configs.recommended,
      ...svelte.configs['flat/recommended'],
      {
        rules: {
          // Svelte 5 特定规则
          'svelte/no-unused-svelte-ignore': 'error',
        },
      },
      {
        ignores: ['.svelte-kit/**', 'build/**', 'node_modules/**'],
      }
    );
    ```

    4. `apps/desktop-ui/.editorconfig`:
    ```ini
    root = true

    [*]
    charset = utf-8
    end_of_line = lf
    insert_final_newline = true
    trim_trailing_whitespace = true

    [*.{rs,toml}]
    indent_style = space
    indent_size = 4

    [*.{js,ts,svelte,json,yml,yaml}]
    indent_style = space
    indent_size = 2
    ```
  </action>
  <verify>
    <automated>test -f apps/desktop-ui/.prettierrc && test -f apps/desktop-ui/eslint.config.js && echo "FRONTEND_TOOLCHAIN_OK"</automated>
  </verify>
  <done>前端 .prettierrc、eslint.config.js、.editorconfig 已创建</done>
</task>

</tasks>

<verification>
- [ ] rust-toolchain.toml channel = "1.82.0"
- [ ] rustfmt.toml edition = "2021"
- [ ] .prettierrc 使用 prettier-plugin-svelte
- [ ] eslint.config.js 使用 flat config 格式
- [ ] .editorconfig 区分 Rust（4空格）和前端（2空格）
</verification>

<success_criteria>
- Rust 工具链配置文件齐全（3 个）
- 前端工具链配置文件齐全（4 个）
- 配置语法正确，可被工具解析
</success_criteria>

<output>
完成后创建 `.planning/phases/00-moonrepo-monorepo/00-05-SUMMARY.md`
</output>
