---
phase: 00-moonrepo-monorepo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .moon/workspace.yml
  - .moon/toolchains.yml
  - .gitignore
  - apps/desktop-ui/.gitkeep
  - crates/domain/.gitkeep
  - crates/application/.gitkeep
  - crates/github_adapter/.gitkeep
  - crates/persistence_sqlite/.gitkeep
  - crates/notification_adapter/.gitkeep
  - crates/runtime_tauri/.gitkeep
  - crates/runtime_server/.gitkeep
  - crates/shared_contracts/.gitkeep
autonomous: true
requirements: []

must_haves:
  truths:
    - "目录结构与 docs/03 架构规范 §5 一致"
    - "moon init 成功，.moon/workspace.yml 存在"
    - "toolchains.yml 配置 Rust + Node.js 版本"
  artifacts:
    - path: ".moon/workspace.yml"
      provides: "moon workspace 配置"
      contains: "projects:"
    - path: ".moon/toolchains.yml"
      provides: "工具链版本锁定"
      contains: "rust:"
    - path: "apps/desktop-ui/"
      provides: "SvelteKit + Tauri 前端目录"
    - path: "crates/domain/"
      provides: "领域层 crate 目录"
  key_links:
    - from: ".moon/workspace.yml"
      to: "apps/* + crates/*"
      via: "projects glob"
      pattern: "projects:"
---

<objective>
## Plan 01: 目录结构与 moon 配置

**Purpose:** 搭建 monorepo 骨架目录和 moon 工程化配置
**Output:** 完整的目录结构 + .moon/workspace.yml + .moon/toolchains.yml
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/03_system_architecture_spec.md

## 架构规范 §5 目录结构

```
/geek-taste
  /apps
    /desktop-ui           # SvelteKit + Tauri frontend shell
  /crates
    /domain               # 纯领域对象与规则
    /application          # 用例编排
    /github_adapter       # GitHub REST client + mapping
    /persistence_sqlite   # SQLite repository impl
    /notification_adapter # 桌面通知
    /runtime_tauri        # Tauri commands / bootstrap
    /runtime_server       # Axum (future)
    /shared_contracts     # JSON schema / DTO / enum export
  /docs                   # 已存在
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建目录结构</name>
  <files>apps/desktop-ui/.gitkeep, crates/domain/.gitkeep, crates/application/.gitkeep, crates/github_adapter/.gitkeep, crates/persistence_sqlite/.gitkeep, crates/notification_adapter/.gitkeep, crates/runtime_tauri/.gitkeep, crates/runtime_server/.gitkeep, crates/shared_contracts/.gitkeep</files>
  <action>
    按 docs/03 架构规范 §5 创建完整目录结构：
    1. 创建 `apps/desktop-ui/` 目录
    2. 创建以下 crates 目录：`domain`, `application`, `github_adapter`, `persistence_sqlite`, `notification_adapter`, `runtime_tauri`, `runtime_server`, `shared_contracts`
    3. 每个目录中放 `.gitkeep` 文件（占位，后续被实际 Cargo.toml 替代）
    4. 目录命名使用下划线（snake_case），与 Cargo crate 命名规范一致
  </action>
  <verify>
    <automated>ls apps/ crates/ 确认所有目录存在</automated>
  </verify>
  <done>所有 9 个目录（1 apps + 8 crates）已创建，每个有 .gitkeep 占位文件</done>
</task>

<task type="auto">
  <name>Task 2: 初始化 moon workspace</name>
  <files>.moon/workspace.yml, .moon/toolchains.yml, .gitignore</files>
  <action>
    手动创建 moon 配置文件（不依赖 `moon init` CLI，避免交互式提示）：

    1. 创建 `.moon/workspace.yml`：
    ```yaml
    projects:
      - 'apps/*'
      - 'crates/*'
    vcs:
      client: 'git'
      defaultBranch: 'main'
    ```

    2. 创建 `.moon/toolchains.yml`：
    ```yaml
    rust:
      version: '1.82.0'
    node:
      version: '22.11.0'
      packageManager: 'pnpm'
    ```

    3. 确保 `.gitignore` 包含：
    ```
    .moon/cache
    target/
    node_modules/
    ```
    如果 `.gitignore` 不存在则创建；如果存在则追加缺失条目。
  </action>
  <verify>
    <automated>test -f .moon/workspace.yml && test -f .moon/toolchains.yml && echo "MOON_OK"</automated>
  </verify>
  <done>.moon/workspace.yml 和 .moon/toolchains.yml 已创建，.gitignore 包含必要忽略规则</done>
</task>

</tasks>

<verification>
- [ ] 目录结构与 docs/03 §5 完全一致
- [ ] .moon/workspace.yml projects globs 覆盖 apps/ 和 crates/
- [ ] .moon/toolchains.yml 配置 Rust 1.82.0 + Node 22.11.0
- [ ] .gitignore 包含 .moon/cache, target/, node_modules/
</verification>

<success_criteria>
- 9 个目录全部存在（apps/desktop-ui + 8 crates）
- .moon/workspace.yml 可解析
- .moon/toolchains.yml 可解析
</success_criteria>

<output>
完成后创建 `.planning/phases/00-moonrepo-monorepo/00-01-SUMMARY.md`
</output>
