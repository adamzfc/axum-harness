---
phase: 10.1-docs-storage-compile-requirements-md-turso-only
plan: 04
type: execute
wave: 3
depends_on: [10.1-01, 10.1-02, 10.1-03]
files_modified:
  - apps/client/web/app/tests/fixtures/tenant.ts
  - apps/client/web/app/tests/e2e/tenant-isolation.test.ts
  - e2e-desktop-playwright/tests/fixtures/tenant.ts
  - e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts
autonomous: true
requirements: [MTEN-01, MTEN-03]
must_haves:
  truths:
    - "固定 tenant_a_user / tenant_b_user 可稳定初始化并重复执行"
    - "settings/theme/counter/agent 四类隔离断言都可自动化验证"
    - "任一 tenant/user 初始化或断言失败时测试立即 fail-fast"
    - "默认 Web/Desktop E2E 均能产出可诊断证据"
  artifacts:
    - path: "apps/client/web/app/tests/e2e/tenant-isolation.test.ts"
      provides: "Web 四维隔离断言"
    - path: "e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts"
      provides: "Desktop 四维隔离断言"
    - path: "apps/client/web/app/tests/fixtures/tenant.ts"
      provides: "Web fail-fast tenant helper"
    - path: "e2e-desktop-playwright/tests/fixtures/tenant.ts"
      provides: "Desktop fail-fast tenant helper"
  key_links:
    - from: "apps/client/web/app/tests/fixtures/tenant.ts"
      to: "apps/client/web/app/tests/e2e/tenant-isolation.test.ts"
      via: "init/reset fixture"
      pattern: "resetTenantPair"
    - from: "e2e-desktop-playwright/tests/fixtures/tenant.ts"
      to: "e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts"
      via: "fixed tenant pair + fail-fast"
      pattern: "tenant_a_user|tenant_b_user"
---

<objective>
扩展多租户测试体系到四类隔离维度并固化 fail-fast + 证据输出。

Purpose: 落实 D-13/D-14/D-15/D-16，并把 Phase 10 的固定双租户策略升级为本阶段完整回归基线。
Output: Web/Desktop 双栈固定 tenant 测试夹具与四维隔离断言更新。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/STORAGE_COMPILE_REQUIREMENTS.md
@.planning/phases/10.1-docs-storage-compile-requirements-md-turso-only/10.1-CONTEXT.md
@.planning/phases/10-multi-tenant-repeatable-verification-channel/10-CONTEXT.md
@apps/client/web/app/tests/fixtures/tenant.ts
@apps/client/web/app/tests/e2e/tenant-isolation.test.ts
@e2e-desktop-playwright/tests/fixtures/tenant.ts
@e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 Web tenant harness 到 settings/theme/counter/agent 四维隔离</name>
  <read_first>apps/client/web/app/tests/fixtures/tenant.ts, apps/client/web/app/tests/e2e/tenant-isolation.test.ts, .planning/phases/10-multi-tenant-repeatable-verification-channel/10-CONTEXT.md</read_first>
  <files>apps/client/web/app/tests/fixtures/tenant.ts, apps/client/web/app/tests/e2e/tenant-isolation.test.ts</files>
  <behavior>
    - Test 1: tenant-A 修改 counter 后 tenant-B 不变。
    - Test 2: tenant/user 维度 settings 与 agent 会话互不污染。
    - Test 3: theme 偏好按用户维度隔离。
    - Test 4: 任一 init/reset/assert 失败立即退出并带 tenant label。
  </behavior>
  <action>在 Web fixture 中保留固定 `tenant_a_user` / `tenant_b_user`（D-13），并将 `tenant-isolation.test.ts` 扩展为四维断言（D-14）：settings 使用现有 store/配置接口验证 tenant+user 隔离；theme 验证按用户维度本地状态隔离；counter 保持 tenant 维度重复运行；agent 验证 tenant+user 会话/配置隔离。所有 helper 与断言失败信息必须包含 tenant label 并立即 fail-fast（D-16）。测试身份仍走测试桩/受控通道，但 helper 命名与签名保持可替换为真实认证实现（D-15）。</action>
  <acceptance_criteria>- Web tenant fixture 中仍使用固定 `tenant_a_user` / `tenant_b_user`。
- `tenant-isolation.test.ts` 同时包含 settings/theme/counter/agent 断言或明确的 helper 调用。
- 错误文案包含 tenant label。
- `bun run test:e2e --project=desktop-chrome tenant-isolation.test.ts` 通过。</acceptance_criteria>
  <verify>
    <automated>cd apps/client/web/app && bun run test:e2e --project=desktop-chrome tests/e2e/tenant-isolation.test.ts</automated>
  </verify>
  <done>Web 多租户回归已覆盖四维隔离并遵守 fail-fast。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 Desktop tenant harness 到四维隔离并保持证据契约</name>
  <read_first>e2e-desktop-playwright/tests/fixtures/tenant.ts, e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts, .github/workflows/e2e-tests.yml</read_first>
  <files>e2e-desktop-playwright/tests/fixtures/tenant.ts, e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts</files>
  <behavior>
    - Test 1: desktop 固定 tenant pair 与 web 一致。
    - Test 2: settings/theme/counter/agent 四维隔离在 desktop 可重复执行。
    - Test 3: runtime readiness / tenant init 失败立即中止，不弱化为继续汇总。
  </behavior>
  <action>在 Desktop Playwright fixture/spec 中延续与 Web 完全一致的固定 tenant pair（D-13），补齐 settings/theme/agent 隔离断言（D-14），并保持 counter repeated-run baseline。沿用 `ensureApiReady` / `withRetry` 模式，但最终失败必须立即抛错结束，不允许“继续跑完再汇总”（D-16）。保持测试桩身份契约可替换为真实邮箱/密码认证入口（D-15），并确保现有 artifact 输出目录与 job-scoped 证据链不被破坏。</action>
  <acceptance_criteria>- Desktop spec 中存在四维隔离断言或其 helper 调用。
- 固定 tenant pair 与 Web 一致。
- 失败路径仍立即抛错，未引入 continue-on-error 语义。
- `cd e2e-desktop-playwright && bun run test:desktop:core --grep "Tenant Isolation"` 通过。</acceptance_criteria>
  <verify>
    <automated>cd e2e-desktop-playwright && bun run test:desktop:core --grep "Tenant Isolation"</automated>
  </verify>
  <done>Desktop 多租户回归已覆盖四维隔离并保留审计级证据契约。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness→auth stub | 测试身份入口属于受控认证边界，需避免不可替换耦合 |
| tenant A→tenant B data path | 跨租户读写是主要数据隔离风险边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-07 | I | web/desktop tenant fixtures | mitigate | 固定 tenant pair + tenant label diagnostics，快速识别泄漏方向 |
| T-10.1-08 | T | auth test stub contract | mitigate | helper API 保持可替换签名，不把 mock-only 细节写死到业务断言 |
| T-10.1-09 | D | repeated-run isolation suite | mitigate | serial + reset baseline + fail-fast，避免脏状态扩散后产生误诊 |
</threat_model>

<verification>
- `cd apps/client/web/app && bun run test:e2e --project=desktop-chrome tests/e2e/tenant-isolation.test.ts`
- `cd e2e-desktop-playwright && bun run test:desktop:core --grep "Tenant Isolation"`
</verification>

<success_criteria>
- Web/Desktop 都覆盖四维隔离断言。
- 固定 tenant pair 与 fail-fast 语义保持一致。
- 默认证据链路仍可用于失败诊断。
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-docs-storage-compile-requirements-md-turso-only/10.1-04-SUMMARY.md`
</output>
