---
phase: 10.1-docs-storage-compile-requirements-md-turso-only
plan: 03
type: execute
wave: 2
depends_on: [10.1-01, 10.1-02]
files_modified:
  - servers/api/Cargo.toml
  - Cargo.toml
  - moon.yml
  - .github/workflows/e2e-tests.yml
autonomous: true
requirements: [MTEN-03]
must_haves:
  truths:
    - "默认开发/测试/CI 编译链路仅包含 Turso 存储路径"
    - "Surreal/libsql 仅在显式 feature 下参与编译"
    - "默认 E2E 门禁不会触发非 Turso adapter 编译链路"
  artifacts:
    - path: "servers/api/Cargo.toml"
      provides: "runtime_server 默认 feature 收敛"
    - path: "moon.yml"
      provides: "默认任务链路仅跑 Turso gate"
    - path: ".github/workflows/e2e-tests.yml"
      provides: "CI 默认 lane 的 Turso-only 验证口径"
  key_links:
    - from: "servers/api/Cargo.toml"
      to: "moon.yml"
      via: "default feature build commands"
      pattern: "cargo check --workspace"
---

<objective>
完成默认编译链路与 CI/E2E 门禁的 Turso-only 裁剪，保留非默认 adapter 的可选扩展位。

Purpose: 落实 D-05/D-06/D-07/D-08，降低默认链路编译开销与误触发风险。
Output: Cargo feature、moon 任务与 CI workflow 完成默认面收敛。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/STORAGE_COMPILE_REQUIREMENTS.md
@.planning/phases/10.1-docs-storage-compile-requirements-md-turso-only/10.1-CONTEXT.md
@servers/api/Cargo.toml
@Cargo.toml
@moon.yml
@.github/workflows/e2e-tests.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 runtime_server 存储 feature 策略（Turso 默认，其他可选）</name>
  <read_first>servers/api/Cargo.toml, Cargo.toml, docs/STORAGE_COMPILE_REQUIREMENTS.md</read_first>
  <files>servers/api/Cargo.toml, Cargo.toml</files>
  <behavior>
    - Test 1: 默认 `cargo check -p runtime_server` 不编译 storage_surreal/libsql 路径。
    - Test 2: 显式 `--features storage-surreal` 可单独编译（保留扩展位）。
    - Test 3: 未启用 feature 时不存在硬依赖引用。
  </behavior>
  <action>在 `servers/api/Cargo.toml` 定义清晰 feature（例如 `storage-turso` 默认开启，`storage-surreal` 可选关闭，D-06）；将 `storage_surrealdb` 调整为 optional 并仅在对应 feature 引用（D-07）；默认 feature 链路只包含 Turso（D-05）。若根 `Cargo.toml` 需配合 workspace 依赖声明，做最小调整并确保无循环/破坏性变更。</action>
  <acceptance_criteria>- `servers/api/Cargo.toml` 存在默认 Turso feature 与可选 surreal feature。
- 默认 `cargo check -p runtime_server` 通过。
- `cargo check -p runtime_server --no-default-features --features storage-surreal` 通过。
- 代码中无未 feature-gate 的 `storage_surrealdb` 强引用。</acceptance_criteria>
  <verify>
    <automated>cargo check -p runtime_server && cargo check -p runtime_server --no-default-features --features storage-surreal</automated>
  </verify>
  <done>runtime_server 默认编译面已固定为 Turso-only，Surreal 路径仅可选启用。</done>
</task>

<task type="auto">
  <name>Task 2: 对齐 moon 与 CI 默认门禁为 Turso-only</name>
  <read_first>moon.yml, .github/workflows/e2e-tests.yml, .planning/phases/10-multi-tenant-repeatable-verification-channel/10-03-SUMMARY.md</read_first>
  <files>moon.yml, .github/workflows/e2e-tests.yml</files>
  <action>更新 `moon.yml` 默认测试/门禁命令与 `.github/workflows/e2e-tests.yml` 默认 lane，明确只验证 Turso 路径（D-08）；保证默认 E2E 不触发 `storage-surreal`/`storage-libsql` 编译。若需保留可选 lane，必须通过显式 feature/job 名称触发，且不影响默认 gate。</action>
  <acceptance_criteria>- `moon.yml` 中默认 gate 命令未显式启用非 Turso storage feature。
- CI 默认工作流不存在编译 Surreal/libsql 的步骤。
- Web/Desktop 默认 E2E lane 仍可执行并保留 artifact 证据上传。</acceptance_criteria>
  <verify>
    <automated>cargo check --workspace && moon run repo:test-e2e && moon run repo:test-desktop</automated>
  </verify>
  <done>默认任务链路与 CI 门禁已对齐 Turso-only，非默认 adapter 不再误入主路径。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo config→build graph | Cargo feature 配置决定哪些存储实现进入默认编译 |
| CI workflow→release gate | 门禁工作流决定默认发布证据是否可信 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-05 | T | servers/api/Cargo.toml features | mitigate | 通过 optional + feature-gate 消除默认强依赖，避免误编译非目标 adapter |
| T-10.1-06 | R | .github/workflows/e2e-tests.yml | mitigate | CI summary 保留 lane 结果和 artifact 命名，确保门禁可审计 |
| T-10.1-07 | D | moon default tasks | mitigate | 默认 gate 严格 Turso-only，减少额外 adapter 编译导致的超时/假红 |
</threat_model>

<verification>
- `cargo check -p runtime_server`
- `cargo check -p runtime_server --no-default-features --features storage-surreal`
- `moon run repo:test-e2e`
- `moon run repo:test-desktop`
</verification>

<success_criteria>
- 默认编译链路仅 Turso。
- 非默认 adapter 仅在显式 feature/job 下编译。
- 默认 CI/E2E 门禁语义稳定且可审计。
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-docs-storage-compile-requirements-md-turso-only/10.1-03-SUMMARY.md`
</output>
