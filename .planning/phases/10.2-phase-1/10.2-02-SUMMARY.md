---
phase: 10.2-phase-1
plan: 02
subsystem: api
tags: [cors, security, tower-http, axum, config]

requires:
  - phase: 10.2-phase-1
    provides: "10.2-01 JWT verification (same security hardening phase)"
provides:
  - Config-driven CORS allowlist with dev permissive fallback
  - `cors_allowed_origins` field on ServerConfig (comma-separated env var)
  - Unit tests for config parsing and CORS layer behavior
affects:
  - "Future plans requiring origin-restricted API access"

tech-stack:
  added: []
  patterns:
    - "Config-driven CORS: empty origins → permissive (dev), set origins → enforced allowlist (prod)"
    - "Custom serde deserializer for comma-separated env var to Vec<String>"
    - "tower_http::cors::AllowOrigin::list() for explicit origin whitelisting"

key-files:
  created: []
  modified:
    - servers/api/src/config.rs - Added cors_allowed_origins field with custom deserializer
    - servers/api/src/lib.rs - Replaced CorsLayer::permissive() with build_cors_layer()

key-decisions:
  - "Used AllowOrigin::list() instead of predicate() for simpler, auditable origin checking"
  - "Empty cors_allowed_origins defaults to permissive mode (dev DX preserved)"
  - "Custom deserializer handles both comma-separated string (env) and array (TOML) inputs"
  - "allow_credentials(true) in enforced mode — requires explicit origins (not wildcard)"

patterns-established:
  - "Env-gated security: empty config → dev permissive, set config → enforced policy"

requirements-completed: [SEC-02]

metrics:
  duration: 6min
  completed: 2026-04-09
---

# Phase 10.2 Plan 02: CORS Configuration Driven Whitelist Summary

**Config-driven CORS allowlist replacing permissive default: empty origins fall back to dev mode, set origins enforce explicit whitelist with credentials**

## Performance

- **Duration:** 6 min
- **Started:** 2026-04-09T08:40:28Z
- **Completed:** 2026-04-09T08:46:18Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments
- Added `cors_allowed_origins: Vec<String>` to `ServerConfig` with custom serde deserializer
- Implemented `build_cors_layer()` function with dev/prod mode switching
- Added 7 unit tests covering config parsing and CORS layer behavior

## Task Commits

1. **Task 1: Add cors_allowed_origins to ServerConfig** - `08c7a72` (feat)
2. **Task 2: Config-driven CORS allowlist** - `f49e8f9` (feat)
3. **Task 3: Unit tests** - `178889a` (test)

## Files Created/Modified
- `servers/api/src/config.rs` - Added `cors_allowed_origins` field with comma-separated deserializer; 3 config tests
- `servers/api/src/lib.rs` - Replaced `CorsLayer::permissive()` with `build_cors_layer()` function; 4 CORS layer tests

## Decisions Made
- Used `AllowOrigin::list()` (tower-http 0.6 API) instead of `AllowOrigin::whitelist()` which doesn't exist in this version
- Empty `cors_allowed_origins` defaults to permissive mode for backward-compatible dev DX
- `allow_credentials(true)` only in enforced mode since wildcard `*` + credentials is rejected by browsers

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- tower-http 0.6 uses `AllowOrigin::list()` not `AllowOrigin::whitelist()` — verified by reading source at `.cargo/registry/src/`
- `toml` crate not available in test scope — used `serde_json` roundtrip for deserialization tests instead
- 14 pre-existing E2E test failures in `http_e2e_test.rs` — confirmed pre-existing (same failures without changes)

## Next Phase Readiness
- CORS is now config-driven and ready for production deployment
- Set `APP_SERVER__CORS_ALLOWED_ORIGINS=https://yourdomain.com` to enforce origin whitelist
- Dev mode (no env var set) remains permissive for local development

---
*Phase: 10.2-phase-1*
*Completed: 2026-04-09*

## Self-Check: PASSED
