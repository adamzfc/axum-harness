---
phase: 10.3-uat-e2e
plan: 03
subsystem: ui
tags: [svelte, localStorage, persistence, agent-chat, settings, web-mode]

requires:
  - phase: 10.3-uat-e2e
    provides: 10.3-01 Tauri IPC bridge, 10.3-02 E2E URL fixes
provides:
  - Settings page localStorage fallback for web mode
  - Agent Chat localStorage fallback for reading settings in web mode
  - Eliminated sendMessage flicker by removing finally block reload
  - Auth id_token persistence via localStorage for counter page in web mode
  - Stable conversation list sorting by created_at descending
affects: [auth, agent-chat, settings, counter]

tech-stack:
  added: []
  patterns: [localStorage fallback for web mode, Tauri/browser dual-path detection]

key-files:
  created: []
  modified:
    - apps/client/web/app/src/routes/(app)/settings/+page.svelte
    - apps/client/web/app/src/routes/(app)/agent/+page.svelte
    - apps/client/web/app/src/lib/ipc/auth.ts
    - apps/client/web/app/src/lib/stores/auth.svelte.ts

key-decisions:
  - "Web mode settings persistence via localStorage with same keys as Tauri Store (settings_api_key, settings_base_url, settings_model)"
  - "Auth token key 'auth_id_token' matches existing counter page convention"
  - "getSession() web fallback returns minimal AuthSession with empty tokens/user but valid id_token"
  - "Removed loadConversations/selectConversation from sendMessage finally block — in-memory state is already correct after optimistic update + streaming"

patterns-established:
  - "localStorage fallback pattern: Tauri path returns early, web mode code follows as default path"

requirements-completed: [UAT-03, UAT-04, UAT-06]

duration: 8min
completed: 2026-04-09
---

# Phase 10.3 Plan 03: Settings 持久化 + Agent Chat 闪烁修复 + 对话排序 Summary

**Web 模式 Settings/Agent localStorage 持久化、sendMessage 去抖动刷新、对话列表稳定排序**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-09T14:10:00Z
- **Completed:** 2026-04-09T14:18:00Z
- **Tasks:** 5
- **Files modified:** 4

## Accomplishments
- Settings 页面在 web 模式下通过 localStorage 持久化 API Key / Base URL / Model，刷新后数据不丢失
- Agent Chat 页面在 web 模式下也能从 localStorage 读取 settings，不再返回空默认值
- sendMessage 的 finally 块移除 loadConversations/selectConversation 调用，消除发送消息后的列表闪烁
- Auth id_token 在 web 模式下持久化到 localStorage，counter 页面可正常读取 Bearer token
- 对话列表按 created_at 降序排序，防止新建对话时列表跳闪

## Task Commits

1. **Task 1: Settings localStorage fallback** - `8269483` (feat)
2. **Task 2: Agent Chat settings fallback** - `12ce1e5` (feat)
3. **Task 3: Remove finally block full reload** - `12d4909` (fix)
4. **Task 4: Auth token web persistence** - `29a3598` (feat)
5. **Task 5: Conversation list sorting** - `fa99376` (fix)

## Files Created/Modified
- `apps/client/web/app/src/routes/(app)/settings/+page.svelte` - loadSettings/saveSettings 添加 localStorage else 分支
- `apps/client/web/app/src/routes/(app)/agent/+page.svelte` - loadSettings 添加 localStorage fallback；loadConversations 排序；finally 块精简
- `apps/client/web/app/src/lib/ipc/auth.ts` - getSession web fallback；clearAuthStore 清除 auth_id_token
- `apps/client/web/app/src/lib/stores/auth.svelte.ts` - setSession 持久化 auth_id_token 到 localStorage

## Decisions Made
- localStorage key 命名与 counter 页面已有约定 (`auth_id_token`) 保持一致，而非计划中的 `auth_id_token`（实际已是同一值）
- getSession web fallback 返回最小 AuthSession 对象，tokens/user 为空值，仅 id_token 有值——counter 页面只需 Bearer token
- 采用 "Tauri path returns early, web fallback follows" 模式而非 else 包裹，避免 LSP 冗余 else 告警

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - LSP] Removed redundant else clause in agent loadSettings**
- **Found during:** Task 2
- **Issue:** LSP reported "else clause can be omitted because previous branches break early"
- **Fix:** Restructured Tauri check to return early, moved web mode fallback after if block as default path
- **Files modified:** apps/client/web/app/src/routes/(app)/agent/+page.svelte
- **Verification:** svelte-check passes with 0 errors

**2. [Rule 1 - Bug] Added missing `picture` field to getSession web fallback UserProfile**
- **Found during:** Task 4
- **Issue:** UserProfile type requires `picture: string` field, but fallback object omitted it
- **Fix:** Added `picture: ''` to the fallback user object
- **Files modified:** apps/client/web/app/src/lib/ipc/auth.ts
- **Verification:** svelte-check passes with 0 errors

**3. [Rule 2 - Missing Critical] Added `auth_id_token` removal to clearAuthStore**
- **Found during:** Task 4
- **Issue:** Counter page reads `auth_id_token` from localStorage, but clearAuthStore only removed `id_token` — logout would leave stale token
- **Fix:** Added `localStorage.removeItem('auth_id_token')` to clearAuthStore else branch
- **Files modified:** apps/client/web/app/src/lib/ipc/auth.ts
- **Verification:** Counter page key consistent across setSession/getSession/clearAuthStore

---

**Total deviations:** 3 auto-fixed (2 bug fixes, 1 missing critical)
**Impact on plan:** All auto-fixes required for type safety and correctness. No scope creep.

## Issues Encountered
None — plan executed with minor type/structure adjustments handled by deviation rules.

## Verification
- ✅ `bun run check` — 0 errors, 0 warnings
- ✅ All 5 tasks committed atomically

## Next Phase Readiness
- Web 模式下 Settings / Agent Chat / Counter 功能闭环，可进入 UAT 验证
- Tauri 桌面模式行为不受影响 (settings 仍走 Tauri Store, getSession 仍走 Tauri IPC)

---
*Phase: 10.3-uat-e2e*
*Completed: 2026-04-09*
