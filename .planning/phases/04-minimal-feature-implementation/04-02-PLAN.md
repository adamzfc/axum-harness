---
phase: 04-minimal-feature-implementation
plan: '02'
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - packages/features/counter/Cargo.toml
  - packages/features/counter/src/lib.rs
  - packages/features/admin/Cargo.toml
  - packages/features/admin/src/lib.rs
  - packages/core/usecases/src/counter_service.rs
  - packages/core/usecases/src/admin_service.rs
  - packages/core/usecases/src/lib.rs
autonomous: true
requirements:
  - COUNTER-01
  - ADMIN-01

must_haves:
  truths:
    - "CounterService trait exists in usecases with increment, decrement, reset, get_value methods"
    - "LibSqlCounterService implements CounterService using LibSqlPort"
    - "AdminService trait exists with get_dashboard_stats method"
    - "LibSqlAdminService implements AdminService using TenantService + CounterService"
    - "Counter table SQL defined: id INTEGER PRIMARY KEY, value INTEGER, updated_at TEXT"
    - "All three new crates/modules compile: feature-counter, feature-admin, usecases updates"
  artifacts:
    - path: "packages/features/counter/Cargo.toml"
      provides: "Counter feature crate manifest"
      contains: "[package]"
    - path: "packages/features/counter/src/lib.rs"
      provides: "Counter usecases (counter operations, no I/O)"
      contains: "pub trait CounterService"
    - path: "packages/features/admin/Cargo.toml"
      provides: "Admin feature crate manifest"
      contains: "[package]"
    - path: "packages/features/admin/src/lib.rs"
      provides: "Admin usecases (dashboard data aggregation)"
      contains: "pub trait AdminService"
    - path: "packages/core/usecases/src/counter_service.rs"
      provides: "CounterService backed by LibSqlPort"
      contains: "LibSqlCounterService"
    - path: "packages/core/usecases/src/admin_service.rs"
      provides: "AdminService backed by TenantService + CounterService"
      contains: "LibSqlAdminService"
  key_links:
    - from: "packages/core/usecases/src/counter_service.rs"
      to: "packages/core/domain/src/ports/lib_sql.rs"
      via: "LibSqlPort::execute and query"
      pattern: "port\\.execute|port\\.query"
    - from: "packages/core/usecases/src/admin_service.rs"
      to: "packages/core/usecases/src/tenant_service.rs"
      via: "TenantService::list_tenants"
      pattern: "tenant_service\\.list_tenants"
    - from: "packages/features/counter/src/lib.rs"
      to: "packages/contracts/api/src/lib.rs"
      via: "shared DTOs"
      pattern: "use contracts_api"
---

<objective>
Create counter and admin feature crates with usecases and LibSQL persistence.

Purpose: Define CounterService (increment/decrement/reset/get_value with LibSQL) and AdminService (dashboard stats from real data). Both features use the hexagonal pattern: feature crate defines domain logic, usecases crate provides LibSQL-backed implementations.
Output: feature-counter, feature-admin crates + usecases counter_service/admin_service modules.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-minimal-feature-implementation/04-CONTEXT.md
@.planning/phases/04-minimal-feature-implementation/04-RESEARCH.md
@packages/core/domain/src/ports/lib_sql.rs
@packages/core/usecases/src/tenant_service.rs
@packages/core/usecases/src/lib.rs
@Cargo.toml

<interfaces>
From packages/core/domain/src/ports/lib_sql.rs:
```rust
pub type LibSqlError = Box<dyn std::error::Error + Send + Sync>;
#[async_trait]
pub trait LibSqlPort: Send + Sync {
    async fn health_check(&self) -> Result<(), LibSqlError>;
    async fn execute(&self, sql: &str, params: Vec<String>) -> Result<u64, LibSqlError>;
    async fn query<T: DeserializeOwned + Send + Sync>(&self, sql: &str, params: Vec<String>) -> Result<Vec<T>, LibSqlError>;
}
```

From packages/core/usecases/src/tenant_service.rs:
```rust
pub struct Tenant { pub id, name, created_at: String }
pub trait TenantService: Send + Sync {
    async fn create_tenant(&self, input: CreateTenantInput) -> Result<Tenant, TenantServiceError>;
    async fn get_tenant(&self, id: &str) -> Result<Option<Tenant>, TenantServiceError>;
    async fn list_tenants(&self) -> Result<Vec<Tenant>, TenantServiceError>;
    async fn delete_tenant(&self, id: &str) -> Result<(), TenantServiceError>;
}
pub struct LibSqlTenantService<P: LibSqlPort> { port: P }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CounterService trait + LibSQL implementation</name>
  <read_first>packages/core/domain/src/ports/lib_sql.rs, packages/core/usecases/src/tenant_service.rs</read_first>
  <files>packages/features/counter/Cargo.toml, packages/features/counter/src/lib.rs, packages/core/usecases/src/counter_service.rs, packages/core/usecases/src/lib.rs</files>
  <action>
    1. Create `packages/features/counter/Cargo.toml`:
    ```toml
    [package]
    name = "feature-counter"
    version = "0.1.0"
    edition = "2021"

    [dependencies]
    domain = { workspace = true }
    usecases = { workspace = true }
    contracts_api = { workspace = true }
    serde = { workspace = true }
    serde_json = { workspace = true }
    async-trait = { workspace = true }
    thiserror = { workspace = true }
    chrono = { workspace = true }
    tracing = { workspace = true }
    ```

    2. Create `packages/features/counter/src/lib.rs`:
    ```rust
    //! Counter feature — increment/decrement/reset with persistence.

    use async_trait::async_trait;
    use serde::{Deserialize, Serialize};

    /// Counter state.
    #[derive(Debug, Clone, Serialize, Deserialize)]
    pub struct Counter {
        pub id: String,
        pub value: i64,
        pub updated_at: String,
    }

    /// Counter operations trait.
    #[async_trait]
    pub trait CounterService: Send + Sync {
        async fn get_value(&self) -> Result<i64, CounterError>;
        async fn increment(&self) -> Result<i64, CounterError>;
        async fn decrement(&self) -> Result<i64, CounterError>;
        async fn reset(&self) -> Result<i64, CounterError>;
    }

    #[derive(Debug, thiserror::Error)]
    pub enum CounterError {
        #[error("Database error: {0}")]
        Database(#[from] Box<dyn std::error::Error + Send + Sync>),
        #[error("Counter not found")]
        NotFound,
    }
    ```

    3. Create `packages/core/usecases/src/counter_service.rs`:
    ```rust
    //! Counter service — LibSQL-backed implementation.

    use async_trait::async_trait;
    use domain::ports::lib_sql::LibSqlPort;
    use feature_counter::{CounterError, CounterService};

    pub const COUNTER_MIGRATION: &str = "CREATE TABLE IF NOT EXISTS counter (id INTEGER PRIMARY KEY, value INTEGER NOT NULL DEFAULT 0, updated_at TEXT NOT NULL DEFAULT (datetime('now')))";

    pub struct LibSqlCounterService<P: LibSqlPort> {
        port: P,
    }

    impl<P: LibSqlPort> LibSqlCounterService<P> {
        pub fn new(port: P) -> Self { Self { port } }
    }

    #[async_trait]
    impl<P: LibSqlPort> CounterService for LibSqlCounterService<P> {
        async fn get_value(&self) -> Result<i64, CounterError> {
            let rows: Vec<(i64,)> = self.port.query("SELECT value FROM counter WHERE id = 1", vec![]).await.map_err(CounterError::Database)?;
            Ok(rows.first().map(|r| r.0).unwrap_or(0))
        }

        async fn increment(&self) -> Result<i64, CounterError> {
            self.port.execute("INSERT INTO counter (id, value, updated_at) VALUES (1, 1, datetime('now')) ON CONFLICT(id) DO UPDATE SET value = value + 1, updated_at = datetime('now')", vec![]).await.map_err(CounterError::Database)?;
            self.get_value().await
        }

        async fn decrement(&self) -> Result<i64, CounterError> {
            self.port.execute("UPDATE counter SET value = value - 1, updated_at = datetime('now') WHERE id = 1", vec![]).await.map_err(CounterError::Database)?;
            self.get_value().await
        }

        async fn reset(&self) -> Result<i64, CounterError> {
            self.port.execute("UPDATE counter SET value = 0, updated_at = datetime('now') WHERE id = 1", vec![]).await.map_err(CounterError::Database)?;
            Ok(0)
        }
    }
    ```

    4. Add to `packages/core/usecases/src/lib.rs`: `pub mod counter_service;`
    5. Add to root Cargo.toml workspace members: `"packages/features/counter"`
    6. Add workspace deps: `feature-counter = { path = "packages/features/counter" }`
  </action>
  <verify>
    <automated>cargo check -p feature-counter && cargo check -p usecases</automated>
  </verify>
  <done>
    feature-counter crate compiles with CounterService trait. usecases crate compiles with LibSqlCounterService implementation. Counter table SQL migration string exported.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AdminService trait + real data implementation</name>
  <read_first>packages/core/usecases/src/tenant_service.rs, packages/core/usecases/src/counter_service.rs (just created), packages/features/counter/src/lib.rs</read_first>
  <files>packages/features/admin/Cargo.toml, packages/features/admin/src/lib.rs, packages/core/usecases/src/admin_service.rs, packages/core/usecases/src/lib.rs</files>
  <action>
    1. Create `packages/features/admin/Cargo.toml`:
    ```toml
    [package]
    name = "feature-admin"
    version = "0.1.0"
    edition = "2021"

    [dependencies]
    domain = { workspace = true }
    usecases = { workspace = true }
    feature-counter = { path = "../counter" }
    serde = { workspace = true }
    serde_json = { workspace = true }
    async-trait = { workspace = true }
    thiserror = { workspace = true }
    chrono = { workspace = true }
    tracing = { workspace = true }
    ```

    2. Create `packages/features/admin/src/lib.rs`:
    ```rust
    //! Admin feature — dashboard statistics from real data.

    use async_trait::async_trait;
    use serde::{Deserialize, Serialize};

    /// Dashboard statistics aggregated from real data.
    #[derive(Debug, Clone, Serialize, Deserialize)]
    pub struct DashboardStats {
        pub tenant_count: i64,
        pub counter_value: i64,
        pub last_login: Option<String>,
        pub app_version: String,
    }

    /// Admin operations trait.
    #[async_trait]
    pub trait AdminService: Send + Sync {
        async fn get_dashboard_stats(&self) -> Result<DashboardStats, AdminError>;
    }

    #[derive(Debug, thiserror::Error)]
    pub enum AdminError {
        #[error("Database error: {0}")]
        Database(#[from] Box<dyn std::error::Error + Send + Sync>),
        #[error("Counter error: {0}")]
        Counter(String),
    }
    ```

    3. Create `packages/core/usecases/src/admin_service.rs`:
    ```rust
    //! Admin service — aggregates real data for dashboard.

    use async_trait::async_trait;
    use domain::ports::lib_sql::LibSqlPort;
    use feature_admin::{AdminError, AdminService, DashboardStats};
    use feature_counter::CounterService;
    use crate::tenant_service::TenantService;

    pub struct LibSqlAdminService<P: LibSqlPort, T: TenantService, C: CounterService> {
        port: P,
        tenant_service: T,
        counter_service: C,
    }

    impl<P: LibSqlPort, T: TenantService, C: CounterService> LibSqlAdminService<P, T, C> {
        pub fn new(port: P, tenant_service: T, counter_service: C) -> Self {
            Self { port, tenant_service, counter_service }
        }
    }

    #[async_trait]
    impl<P: LibSqlPort, T: TenantService, C: CounterService> AdminService for LibSqlAdminService<P, T, C> {
        async fn get_dashboard_stats(&self) -> Result<DashboardStats, AdminError> {
            let tenants = self.tenant_service.list_tenants().await
                .map_err(|e| AdminError::Database(Box::new(std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))))?;
            let counter_value = self.counter_service.get_value().await
                .map_err(|e| AdminError::Counter(e.to_string()))?;

            Ok(DashboardStats {
                tenant_count: tenants.len() as i64,
                counter_value,
                last_login: tenants.first().map(|t| t.created_at.clone()),
                app_version: env!("CARGO_PKG_VERSION").to_string(),
            })
        }
    }
    ```

    4. Add to `packages/core/usecases/src/lib.rs`: `pub mod admin_service;`
    5. Add to root Cargo.toml workspace members: `"packages/features/admin"`
    6. Add workspace deps: `feature-admin = { path = "packages/features/admin" }`
  </action>
  <verify>
    <automated>cargo check -p feature-admin && cargo check -p usecases</automated>
  </verify>
  <done>
    feature-admin crate compiles with AdminService trait. usecases crate compiles with LibSqlAdminService that aggregates tenant count + counter value.
  </done>
</task>

</tasks>

<verification>
- `cargo check -p feature-counter` passes
- `cargo check -p feature-admin` passes
- `cargo check -p usecases` passes
- `cargo check --workspace` passes
- `grep -q "features/counter" Cargo.toml` — workspace member
- `grep -q "features/admin" Cargo.toml` — workspace member
- `grep -q "pub trait CounterService" packages/features/counter/src/lib.rs`
- `grep -q "pub trait AdminService" packages/features/admin/src/lib.rs`
</verification>

<success_criteria>
- feature-counter crate: CounterService trait with increment/decrement/reset/get_value
- feature-admin crate: AdminService trait with get_dashboard_stats returning DashboardStats
- usecases: LibSqlCounterService (LibSQL persistence) + LibSqlAdminService (data aggregation)
- Counter migration SQL string exported for use by host crates
- Workspace compiles, hexagonal boundaries preserved
</success_criteria>

<output>
After completion, create `.planning/phases/04-minimal-feature-implementation/04-02-SUMMARY.md`
</output>
