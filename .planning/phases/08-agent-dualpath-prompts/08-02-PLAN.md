---
phase: 08-agent-dualpath-prompts
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - .agents/prompts/add-feature.md
  - .agents/prompts/add-host.md
  - .agents/prompts/refactor-boundary.md
  - .planning/phases/05-agent-friendly/05-VERIFICATION.md
autonomous: true
requirements:
  - AGENT-DEV-01

must_haves:
  truths:
    - ".agents/prompts/ 包含 add-feature、add-host、refactor-boundary 三个 prompt 模板"
    - "Phase 5 VERIFICATION.md 存在且包含已有 playbooks + rubrics 的验证步骤"
    - "VERIFICATION.md 可执行，每条验证有明确的通过/失败判定"
  artifacts:
    - path: ".agents/prompts/add-feature.md"
      provides: "feature 添加 prompt 模板"
      contains: "## Purpose"
    - path: ".agents/prompts/add-host.md"
      provides: "host 添加 prompt 模板"
      contains: "## Purpose"
    - path: ".agents/prompts/refactor-boundary.md"
      provides: "边界重构 prompt 模板"
      contains: "## Purpose"
    - path: ".planning/phases/05-agent-friendly/05-VERIFICATION.md"
      provides: "Phase 5 验证文档"
      contains: "## Verification"
  key_links:
    - from: ".agents/prompts/add-feature.md"
      to: ".agents/playbooks/create-feature.md"
      via: "prompt 引用 playbook 作为参考"
      pattern: "create-feature"
    - from: ".planning/phases/05-agent-friendly/05-VERIFICATION.md"
      to: ".agents/playbooks/*.md"
      via: "验证已有 playbook 文件存在"
      pattern: "playbooks/"
---

<objective>
补全 .agents/prompts/ 目录的三个 prompt 模板，并生成 Phase 5 VERIFICATION.md 验证已有 playbooks + rubrics 的可用性。

Purpose: 完成 AGENT-DEV-01 要求的 prompts 模板，同时为 Phase 5 提供可执行的验证文档。
Output: 三个 prompt 模板文件 + Phase 5 VERIFICATION.md
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agent-dualpath-prompts/08-CONTEXT.md

# 已有 assets — 验证目标

From `.agents/playbooks/`:
- `create-feature.md` — 完整的 feature 创建流程规范（5 步：contracts → domain → usecases → adapter → host/frontend）
- `update-contracts.md` — 完整的 contracts 修改流程规范

From `.agents/rubrics/`:
- `boundary-compliance.md` — 六边形架构边界规则
- `code-review.md` — 代码审查标准
- `task-completion.md` — 任务完成标准

From `.agents/prompts/`:
- 仅 .gitkeep，内容为空（per D-06，用户亲自写，本阶段只确保目录存在）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建三个 prompt 模板到 .agents/prompts/</name>
  <files>
    .agents/prompts/add-feature.md
    .agents/prompts/add-host.md
    .agents/prompts/refactor-boundary.md
  </files>
  <read_first>
    - .agents/playbooks/create-feature.md — 参考其结构和风格
    - .agents/rubrics/boundary-compliance.md — 了解边界约束规则
  </read_first>
  <action>
    创建三个 prompt 模板文件，每个文件遵循统一结构：Purpose、When to Use、Prerequisites、Steps、Verification。

    **add-feature.md** 内容要点：
    - Purpose: 引导 agent 添加新功能模块，从 contract 定义到 frontend 消费
    - 引用 create-feature playbook 作为详细流程参考
    - 包含快速检查清单：contracts DTO 定义、domain port trait、usecases service、adapter 实现、host command、frontend 消费
    - 提醒边界合规：core 不依赖 host，adapters 只做翻译

    **add-host.md** 内容要点：
    - Purpose: 引导 agent 添加新的 Tauri host adapter
    - 步骤：创建 Tauri command（参考 counter.rs 模式）、注册到 commands/mod.rs、注册到 lib.rs invoke_handler、添加必要 Cargo 依赖
    - 提醒：command 通过 AppHandle 获取 state，委托给 usecases service，不直接操作数据库

    **refactor-boundary.md** 内容要点：
    - Purpose: 引导 agent 进行边界重构（如迁移实现到正确层、消除跨层依赖）
    - 引用 boundary-compliance rubric 作为判断标准
    - 步骤：识别违规 import → 确定正确归属层 → 迁移代码 → 更新依赖 → 验证
    - 提醒：保持接口不变，只移动实现位置

    每个文件 30-60 行，简洁实用，不过度设计。
  </action>
  <verify>
    <automated>test -f .agents/prompts/add-feature.md && test -f .agents/prompts/add-host.md && test -f .agents/prompts/refactor-boundary.md && grep -l "## Purpose" .agents/prompts/*.md | wc -l | grep -q "3" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>三个 prompt 文件存在，每个包含 Purpose 章节，内容与对应场景匹配，引用已有 playbook/rubric 作为参考</done>
</task>

<task type="auto">
  <name>Task 2: 生成 Phase 5 VERIFICATION.md</name>
  <files>.planning/phases/05-agent-friendly/05-VERIFICATION.md</files>
  <read_first>
    - .agents/playbooks/create-feature.md — 验证目标
    - .agents/playbooks/update-contracts.md — 验证目标
    - .agents/rubrics/boundary-compliance.md — 验证目标
    - .agents/rubrics/code-review.md — 验证目标
    - .agents/rubrics/task-completion.md — 验证目标
    - .planning/ROADMAP.md — Phase 5 success criteria
  </read_first>
  <action>
    创建 `.planning/phases/05-agent-friendly/05-VERIFICATION.md`，包含以下验证步骤：

    ```markdown
    # Phase 5 Verification: Agent-Friendly 开发基建

    **Date:** 2026-04-02
    **Phase:** 05-agent-friendly
    **Scope:** Verify existing playbooks + rubrics are usable

    ## Verification Steps

    ### 1. File Existence Check

    Run: `find .agents/ -type f -name "*.md" | sort`

    Expected files:
    - [ ] .agents/playbooks/create-feature.md
    - [ ] .agents/playbooks/update-contracts.md
    - [ ] .agents/rubrics/boundary-compliance.md
    - [ ] .agents/rubrics/code-review.md
    - [ ] .agents/rubrics/task-completion.md

    Pass criteria: All 5 files exist.

    ### 2. Format Validation

    For each file, verify it contains required sections:

    **Playbooks** (create-feature.md, update-contracts.md):
    - [ ] Contains `# ` title heading
    - [ ] Contains `## Purpose` or `## Trigger` section
    - [ ] Contains `## Execution Steps` or numbered steps
    - [ ] Contains `## Verification` section

    **Rubrics** (boundary-compliance.md, code-review.md, task-completion.md):
    - [ ] Contains `# ` title heading
    - [ ] Contains clear evaluation criteria (checklist or rules)
    - [ ] Contains pass/fail conditions

    Pass criteria: Each file has title, purpose/trigger, actionable steps, verification.

    ### 3. Content Executability

    **create-feature.md:**
    - [ ] Steps reference actual file paths that exist (e.g., `packages/contracts/api/`)
    - [ ] Mentions `just verify` or `cargo test` as verification commands
    - [ ] References `AGENTS.md` as a prerequisite

    **update-contracts.md:**
    - [ ] Steps reference actual file paths
    - [ ] Mentions typegen (`just typegen`) as a step
    - [ ] Has rollback instructions

    **boundary-compliance.md:**
    - [ ] Defines import rules per layer (domain, usecases, adapters, hosts)
    - [ ] Lists forbidden imports or allowed import graph
    - [ ] Has a method to check violations

    **code-review.md:**
    - [ ] Covers correctness, style, error handling, testing
    - [ ] References project conventions from AGENTS.md
    - [ ] Has a checklist format

    **task-completion.md:**
    - [ ] Defines what "done" means
    - [ ] Includes verification steps
    - [ ] References build/test commands

    Pass criteria: Each file references real paths and executable commands.

    ### 4. Cross-Reference Consistency

    - [ ] Playbooks reference rubrics where appropriate (e.g., create-feature mentions boundary-compliance)
    - [ ] Rubrics reference AGENTS.md conventions
    - [ ] No circular or broken references

    Pass criteria: Cross-references are consistent and valid.

    ## Overall Result

    | Check | Status | Notes |
    |-------|--------|-------|
    | File Existence | ☐ PASS / ☐ FAIL | |
    | Format Validation | ☐ PASS / ☐ FAIL | |
    | Content Executability | ☐ PASS / ☐ FAIL | |
    | Cross-Reference Consistency | ☐ PASS / ☐ FAIL | |

    **Phase 5 Status:** ☐ PASSED / ☐ FAILED

    ## Deferred Items (Not Blocking)

    - Skills content (rust-core, tauri-host, sveltekit-ui, contracts-typegen, testing) — Phase 5 CONTEXT.md D-01 决定跳过
    - Prompts content — Phase 8 补全（本验证执行时）
    ```

    注意：per D-08，skills 和 prompts 标记为 deferred，不阻塞 Phase 5 通过。VERIFICATION.md 只验证已有 playbooks + rubrics。
  </action>
  <verify>
    <automated>test -f .planning/phases/05-agent-friendly/05-VERIFICATION.md && grep -q "## Verification Steps" .planning/phases/05-agent-friendly/05-VERIFICATION.md && grep -q "File Existence" .planning/phases/05-agent-friendly/05-VERIFICATION.md && grep -q "Phase 5 Status" .planning/phases/05-agent-friendly/05-VERIFICATION.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>05-VERIFICATION.md 存在，包含 4 个验证步骤（文件存在性、格式、内容可执行性、交叉引用一致性），明确标注 deferred items，有 Overall Result 表格</done>
</task>

</tasks>

<verification>
- 三个 prompt 文件存在且包含 Purpose 章节
- 05-VERIFICATION.md 存在且包含验证步骤和 Overall Result 表格
- `find .agents/prompts/ -type f -name "*.md" | wc -l` 返回 3（不含 .gitkeep）
- VERIFICATION.md 中所有验证目标文件路径存在
</verification>

<success_criteria>
- [ ] .agents/prompts/add-feature.md 创建，包含 Purpose、Steps、Verification 结构
- [ ] .agents/prompts/add-host.md 创建，包含 Purpose、Steps、Verification 结构
- [ ] .agents/prompts/refactor-boundary.md 创建，包含 Purpose、Steps、Verification 结构
- [ ] .planning/phases/05-agent-friendly/05-VERIFICATION.md 创建，包含 4 个验证步骤
- [ ] VERIFICATION.md 明确标注 skills 和 prompts 为 deferred
- [ ] 所有验证目标文件（playbooks + rubrics）路径正确且可执行
</success_criteria>

<output>
After completion, create `.planning/phases/08-agent-dualpath-prompts/08-02-SUMMARY.md`
</output>
