---
phase: 14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - .github/workflows/e2e-tests.yml
  - moon.yml
  - Justfile
  - e2e-desktop-playwright/package.json
autonomous: false
requirements: [QGATE-01, QGATE-02]
must_haves:
  truths:
    - "Maintainer can observe tauri-playwright desktop results on macOS in CI without replacing existing lanes"
    - "Repository-wide E2E pass/fail is evaluated across WDIO, web Playwright matrix, and new tauri-playwright suite"
    - "Each E2E lane emits downloadable diagnostics artifacts for failure triage"
  artifacts:
    - path: ".github/workflows/e2e-tests.yml"
      provides: "macOS tauri observer job + artifact upload + consolidated summary"
    - path: "e2e-desktop-playwright/package.json"
      provides: "CI script for tauri suite"
    - path: "moon.yml"
      provides: "repo-level full E2E command wiring"
  key_links:
    - from: ".github/workflows/e2e-tests.yml"
      to: "e2e-desktop-playwright/package.json"
      via: "desktop-e2e-playwright-tauri job run command"
      pattern: "desktop-e2e-playwright-tauri"
    - from: "moon.yml"
      to: "Justfile"
      via: "repo full E2E gate task exposure"
      pattern: "test-e2e"
---

<objective>
接通 CI 过渡通道与全量 E2E 验收闭环：新增 macOS tauri-playwright 观察 job，并定义仓库级“全绿才通过”命令。

Purpose: 落地 D-05、D-06、D-07、D-08，确保迁移收益可观测且失败可诊断。
Output: e2e workflow 新观察通道 + 全量 E2E 统一入口 + 可审计 artifacts。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-CONTEXT.md
@.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-RESEARCH.md
@docs/TAURI_PLAYWRIGHT_MIGRATION_CONTEXT.md
@.github/workflows/e2e-tests.yml
@.planning/phases/10-multi-tenant-repeatable-verification-channel/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add desktop-e2e-playwright-tauri macOS observer job (D-05, D-06, D-08)</name>
  <files>.github/workflows/e2e-tests.yml, e2e-desktop-playwright/package.json</files>
  <read_first>
    - .github/workflows/e2e-tests.yml
    - e2e-desktop-playwright/package.json
    - e2e-tests/package.json
    - apps/client/web/app/playwright.config.ts
    - .planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-CONTEXT.md
  </read_first>
  <action>在 `e2e-tests.yml` 新增 `desktop-e2e-playwright-tauri` job，先锁定 `macos-latest` 观察通道（per D-05）。该 job 安装 Rust/Bun/Playwright 依赖并运行新套件 CI 脚本，不替换也不移除现有 `desktop-e2e` (WDIO) 与 `web-e2e`（per D-06）。新增 artifacts 上传（playwright-report、test-results、视频/截图等），命名采用 job-scoped 格式并设置 `retention-days: 7`（per D-08）。</action>
  <acceptance_criteria>
    - workflow contains job key `desktop-e2e-playwright-tauri` with `runs-on: macos-latest`.
    - existing jobs `desktop-e2e` and `web-e2e` remain present.
    - new job has `actions/upload-artifact@v4` step with retention days set.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "desktop-e2e-playwright-tauri\|macos-latest\|upload-artifact\|retention-days" .github/workflows/e2e-tests.yml</automated>
  </verify>
  <done>CI 具备 tauri-playwright 观察能力，且仍保持双栈并行回滚路径。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire repository-level full E2E gate command (D-07)</name>
  <files>moon.yml, Justfile, e2e-desktop-playwright/package.json</files>
  <read_first>
    - moon.yml
    - Justfile
    - e2e-tests/package.json
    - apps/client/web/app/package.json
    - e2e-desktop-playwright/package.json
    - .planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-CONTEXT.md
  </read_first>
  <action>新增仓库级“全量 E2E”命令入口（per D-07）：串行运行 `e2e-tests test:ci`（WDIO 全套）、`apps/client/web/app test:e2e`（Playwright 全项目矩阵）、`e2e-desktop-playwright test:ci`（新 tauri Phase1）。在 `moon.yml` 增加 repo task（例如 `test-e2e-full`），并在 `Justfile` 暴露 `test-e2e-full` 命令；同时在新套件 `package.json` 明确定义 `test:ci`，确保 workflow 和本地命令一致。</action>
  <acceptance_criteria>
    - moon.yml contains a task that runs all three E2E tracks.
    - Justfile exposes human-facing command for the full E2E gate.
    - e2e-desktop-playwright/package.json contains `test:ci` script.
  </acceptance_criteria>
  <verify>
    <automated>rtk just test-e2e-full</automated>
  </verify>
  <done>“跑通 E2E”从口头标准变成可执行仓库命令，覆盖三条测试通道。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification for CI three-lane evidence and full gate command</name>
  <files>.github/workflows/e2e-tests.yml, moon.yml, Justfile</files>
  <action>在自动化改动完成后，人工确认 CI 页面与仓库命令层面都满足三通道并行与证据可追溯要求，再批准进入下一阶段。</action>
  <what-built>新增 macOS tauri-playwright 观察 job、全量 E2E gate 命令、三通道 artifacts 约束</what-built>
  <how-to-verify>
    1. 在 PR 或分支触发 `.github/workflows/e2e-tests.yml` 一次。
    2. 确认 workflow 页面同时出现：`desktop-e2e`、`web-e2e`、`desktop-e2e-playwright-tauri`。
    3. 在 run artifacts 中检查三类证据包均可下载（WDIO、web Playwright、tauri-playwright）。
    4. 本地执行 `just test-e2e-full`，确认命令确实串行执行三条 E2E 轨道。
  </how-to-verify>
  <verify>
    <automated>MISSING — Wave 0 must create a CI branch-protection assertion script first</automated>
  </verify>
  <done>人工确认三条 E2E 轨道与 artifacts 均可见、可下载、可用于故障诊断，并给出 approved/issue 反馈。</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → artifact storage | 失败证据进入可下载存储，需可诊断且不泄露敏感内容 |
| Multi-lane E2E status → merge decision | 合并判断依赖三条测试轨道的真实状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | R | `.github/workflows/e2e-tests.yml` summary | mitigate | Include tauri observer lane explicitly in workflow summary table |
| T-14-08 | I | artifact upload steps | mitigate | Upload diagnostic-only paths (reports/results/media) with 7-day retention |
| T-14-09 | D | full E2E gate command | mitigate | Provide single deterministic command path (`just test-e2e-full`) to avoid partial green misuse |
</threat_model>

<verification>
- `grep -n "desktop-e2e-playwright-tauri\|retention-days\|upload-artifact" .github/workflows/e2e-tests.yml`
- `rtk just test-e2e-full`
</verification>

<success_criteria>
- D-05, D-06, D-07, D-08 在 CI 和本地命令层面同时落地。
- 失败时具备可追踪证据，成功时具备三通道全绿判定依据。
</success_criteria>

<output>
After completion, create `.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-03-SUMMARY.md`
</output>
