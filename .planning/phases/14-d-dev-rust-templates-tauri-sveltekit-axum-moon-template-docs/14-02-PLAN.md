---
phase: 14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - e2e-desktop-playwright/tests/fixtures/auth.ts
  - e2e-desktop-playwright/tests/fixtures/tenant.ts
  - e2e-desktop-playwright/tests/specs/login.spec.ts
  - e2e-desktop-playwright/tests/specs/counter.spec.ts
  - e2e-desktop-playwright/tests/specs/smoke.spec.ts
  - e2e-desktop-playwright/package.json
autonomous: true
requirements: [QGATE-01]
must_haves:
  truths:
    - "Maintainer can run migrated desktop smoke/login/counter tests in tauri mode"
    - "Migrated tests preserve existing mock auth and tenant identity semantics"
    - "WDIO suite remains runnable as rollback path"
  artifacts:
    - path: "e2e-desktop-playwright/tests/specs/login.spec.ts"
      provides: "desktop login assertions migrated from WDIO baseline"
    - path: "e2e-desktop-playwright/tests/specs/counter.spec.ts"
      provides: "desktop counter interaction assertions with tenant-safe setup"
    - path: "e2e-desktop-playwright/tests/fixtures/tenant.ts"
      provides: "stable tenant identity fixture aligned with existing suites"
  key_links:
    - from: "e2e-desktop-playwright/tests/fixtures/auth.ts"
      to: "apps/client/web/app/tests/fixtures/auth.ts"
      via: "same deep-link mock callback semantics"
      pattern: "deep-link://new-url"
    - from: "e2e-desktop-playwright/tests/fixtures/tenant.ts"
      to: "e2e-tests/helpers/tenant.mjs"
      via: "tenant_a_user / tenant_b_user identity parity"
      pattern: "tenant_a_user|tenant_b_user"
---

<objective>
完成 Phase 1 首批迁移用例（smoke/login/counter），并确保新套件认证/租户语义与现有基线一致。

Purpose: 在不触碰业务逻辑的前提下实现 D-01、D-02、D-04、D-06 的可执行迁移深度。
Output: tauri-playwright Phase 1 测试集合（smoke/login/counter）和可重复运行脚本。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-CONTEXT.md
@.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-RESEARCH.md
@docs/TAURI_PLAYWRIGHT_MIGRATION_CONTEXT.md
@.planning/phases/10-multi-tenant-repeatable-verification-channel/10-03-SUMMARY.md

<interfaces>
From `apps/client/web/app/tests/fixtures/auth.ts`:
```ts
export async function triggerMockOAuth(page: Page, mockCode: string = 'mock_auth_code')
```

From `apps/client/web/app/tests/fixtures/tenant.ts`:
```ts
export const TENANT_A = { userSub: 'tenant_a_user' }
export const TENANT_B = { userSub: 'tenant_b_user' }
```

From `e2e-tests/helpers/tenant.mjs`:
```js
export const TENANT_1 = { userSub: 'tenant_a_user' }
export const TENANT_2 = { userSub: 'tenant_b_user' }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port auth/tenant fixtures into new desktop Playwright suite (D-04)</name>
  <files>e2e-desktop-playwright/tests/fixtures/auth.ts, e2e-desktop-playwright/tests/fixtures/tenant.ts, e2e-desktop-playwright/package.json</files>
  <read_first>
    - apps/client/web/app/tests/fixtures/auth.ts
    - apps/client/web/app/tests/fixtures/tenant.ts
    - e2e-tests/helpers/tenant.mjs
    - e2e-desktop-playwright/tests/fixtures/tauri.ts
    - .planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: mock deep-link login helper can place app in authenticated state without real OAuth.
    - Test 2: tenant fixtures expose stable tenant_a_user and tenant_b_user identities.
    - Test 3: fixture helpers can reset baseline for deterministic counter assertions.
  </behavior>
  <action>在新套件中实现 `fixtures/auth.ts` 与 `fixtures/tenant.ts`，语义与既有 web/desktop 基线一致（per D-04）：保留 deep-link mock callback 机制、固定 tenant identity（`tenant_a_user`/`tenant_b_user`）和可重复 reset helper。新增 package 脚本 `test:phase1` 仅运行 smoke/login/counter，避免与后续 Phase 2/3 用例耦合（per D-01, D-02）。</action>
  <acceptance_criteria>
    - new auth fixture contains deep-link callback trigger function.
    - new tenant fixture contains both `tenant_a_user` and `tenant_b_user` constants.
    - package.json contains script `test:phase1` targeting smoke/login/counter specs only.
  </acceptance_criteria>
  <verify>
    <automated>rtk bun run --cwd e2e-desktop-playwright test:phase1</automated>
  </verify>
  <done>新套件 fixture 语义与现有认证/租户测试约定保持一致。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate smoke/login/counter specs into tauri-playwright suite (D-01, D-02, D-10)</name>
  <files>e2e-desktop-playwright/tests/specs/smoke.spec.ts, e2e-desktop-playwright/tests/specs/login.spec.ts, e2e-desktop-playwright/tests/specs/counter.spec.ts</files>
  <read_first>
    - e2e-tests/specs/smoke.e2e.mjs
    - e2e-tests/specs/login.e2e.mjs
    - e2e-tests/specs/counter.e2e.mjs
    - e2e-desktop-playwright/tests/fixtures/auth.ts
    - e2e-desktop-playwright/tests/fixtures/tenant.ts
  </read_first>
  <behavior>
    - Test 1: smoke validates desktop runtime shell loads.
    - Test 2: login validates guard and login page expectations under mock auth assumptions.
    - Test 3: counter validates increment/decrement/reset basic interactions with deterministic setup.
  </behavior>
  <action>将 `e2e-tests/specs/{smoke,login,counter}.e2e.mjs` 迁移为 Playwright 版本到新套件，并保持断言语义一致（per D-01, D-02）。禁止通过修改产品逻辑来适配测试（per D-10）；若断言不稳定，优先改 selector/等待策略与 fixture 辅助。保留原 WDIO spec 不删除、不重命名（per D-06）。</action>
  <acceptance_criteria>
    - smoke/login/counter specs exist under e2e-desktop-playwright/tests/specs.
    - each migrated spec has at least one assertion equivalent to original WDIO behavior.
    - files under e2e-tests/specs/smoke.e2e.mjs, login.e2e.mjs, counter.e2e.mjs remain present.
  </acceptance_criteria>
  <verify>
    <automated>rtk bun run --cwd e2e-desktop-playwright test:phase1</automated>
  </verify>
  <done>Phase 1 迁移用例在新套件可执行通过，且旧 WDIO 回滚路径仍在。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture auth mocks → app auth flow | 测试桩不得改变实际产品认证语义 |
| Tenant fixture identities → API tenant isolation | 固定身份映射必须与现有回归语义一致 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | T | migrated fixtures | mitigate | Keep deep-link and tenant identities identical to existing fixtures |
| T-14-05 | I | tenant fixture | mitigate | Reuse stable `tenant_a_user`/`tenant_b_user` and avoid ad-hoc random IDs |
| T-14-06 | R | migrated specs | mitigate | Preserve equivalent assertions to WDIO baseline for traceable parity |
</threat_model>

<verification>
- `rtk bun run --cwd e2e-desktop-playwright test:phase1`
- `rtk bun run --cwd e2e-tests test:ci`
</verification>

<success_criteria>
- D-01, D-02, D-04, D-06 在测试层面落地且可自动验证。
- 新旧 desktop 套件可并行运行，迁移保持可回滚。
</success_criteria>

<output>
After completion, create `.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-02-SUMMARY.md`
</output>
