---
phase: 14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs
plan: 05
type: execute
wave: 5
depends_on: ["04"]
files_modified:
  - e2e-desktop-playwright/tests/specs/admin.spec.ts
  - e2e-desktop-playwright/tests/specs/agent.spec.ts
  - e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts
  - e2e-desktop-playwright/package.json
  - e2e-desktop-playwright/playwright.config.ts
  - moon.yml
  - Justfile
  - .github/workflows/e2e-tests.yml
  - e2e-tests/package.json
  - e2e-tests/wdio.conf.mjs
  - e2e-tests/scripts/run-desktop-e2e.mjs
autonomous: true
gap_closure: true
requirements: [QGATE-01, QGATE-02]
must_haves:
  truths:
    - "Legacy WDIO stack is fully deprecated and no longer participates in E2E gate"
    - "All required desktop scenarios run under e2e-desktop-playwright"
    - "Repository full E2E gate remains diagnosable after WDIO retirement"
  artifacts:
    - path: "e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts"
      provides: "tenant-1 and tenant-2 isolation semantics migrated into tauri-playwright execution path"
    - path: "moon.yml"
      provides: "full E2E command excludes WDIO lane and keeps deterministic pass/fail aggregation"
    - path: ".github/workflows/e2e-tests.yml"
      provides: "CI no longer runs WDIO lane and still uploads desktop/web diagnostics artifacts"
  key_links:
    - from: "Justfile"
      to: "moon.yml"
      via: "test-e2e-full command exposure"
      pattern: "test-e2e-full"
    - from: "moon.yml"
      to: "e2e-desktop-playwright/package.json"
      via: "desktop lane script entrypoint"
      pattern: "e2e-desktop-playwright"
    - from: ".github/workflows/e2e-tests.yml"
      to: "e2e-desktop-playwright/package.json"
      via: "desktop tauri CI job command"
      pattern: "bun run test:ci"
---

<objective>
将 Phase 14 的桌面 E2E 迁移收敛到单栈：弃用 `e2e-tests` (WDIO + tauri-driver) 并以 `e2e-desktop-playwright` 作为唯一桌面 E2E 实现。

Purpose: 以用户新约束覆盖 D-06 的“长期并行”策略，完成旧栈退场与必要用例迁移，同时保持 D-07 全仓 E2E 口径、D-08 证据可诊断、D-10 不改业务行为。
Output: 可执行且可诊断的双通道全仓门禁（web Playwright + desktop tauri-playwright），以及已弃用的 WDIO 旧栈。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-CONTEXT.md
@.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-VERIFICATION.md
@.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-04-PLAN.md
@docs/TAURI_PLAYWRIGHT_MIGRATION_CONTEXT.md

<interfaces>
From `e2e-desktop-playwright/package.json`:
```json
"scripts": {
  "test:ci": "playwright test --project=tauri"
}
```

From `moon.yml`:
```yaml
test-e2e-full:
  # aggregate all repository E2E lanes
```

From `.github/workflows/e2e-tests.yml`:
```yaml
jobs:
  web-e2e:
  desktop-e2e-playwright-tauri:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate remaining required WDIO desktop specs into tauri-playwright (D-10)</name>
  <files>e2e-desktop-playwright/tests/specs/admin.spec.ts, e2e-desktop-playwright/tests/specs/agent.spec.ts, e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts, e2e-desktop-playwright/package.json</files>
  <read_first>
    - e2e-tests/specs/admin.e2e.mjs
    - e2e-tests/specs/agent.e2e.mjs
    - e2e-tests/specs/tenant-isolation.e2e.mjs
    - e2e-desktop-playwright/tests/fixtures/auth.ts
    - e2e-desktop-playwright/tests/fixtures/tenant.ts
    - e2e-desktop-playwright/tests/specs/login.spec.ts
    - e2e-desktop-playwright/tests/specs/counter.spec.ts
  </read_first>
  <action>将仍有价值的 WDIO 桌面用例（admin、agent、tenant-isolation）迁移到 `e2e-desktop-playwright`，保持认证守卫、租户身份与断言语义等价，不通过修改业务逻辑来“迎合测试”。在 package 脚本中增加可用于迁移回归的分组命令（例如 `test:desktop:core`），确保必要场景被单栈覆盖。</action>
  <acceptance_criteria>
    - `e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts` 存在并覆盖 tenant_a_user / tenant_b_user 隔离语义。
    - admin/agent 至少各有一个迁移后的 tauri-playwright spec，且断言不弱化。
    - `rtk bun run --cwd e2e-desktop-playwright test:ci` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk bun run --cwd e2e-desktop-playwright test:ci</automated>
  </verify>
  <done>桌面必要测试不再依赖 WDIO 代码路径，全部在 tauri-playwright 套件可执行通过。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove WDIO lane from repository gate and CI while keeping diagnostics (QGATE-01, QGATE-02)</name>
  <files>moon.yml, Justfile, .github/workflows/e2e-tests.yml, e2e-tests/package.json, e2e-tests/wdio.conf.mjs, e2e-tests/scripts/run-desktop-e2e.mjs</files>
  <read_first>
    - moon.yml
    - Justfile
    - .github/workflows/e2e-tests.yml
    - e2e-tests/package.json
    - e2e-tests/wdio.conf.mjs
    - e2e-tests/scripts/run-desktop-e2e.mjs
  </read_first>
  <action>从仓库全量 E2E 入口与 CI workflow 中移除 WDIO lane：`test-e2e-full` 改为仅聚合 web Playwright 与 desktop tauri-playwright；workflow 删除 `desktop-e2e` job 与 WDIO artifact 步骤，保留并强化现有 Playwright artifacts 上传。随后弃用 `e2e-tests` 目录中的入口/配置（删除或标记 retired，按仓库约定选择一种），确保没有活动脚本再引用 WDIO。</action>
  <acceptance_criteria>
    - `moon.yml` 的 `test-e2e-full` 不再调用 `e2e-tests`。
    - `.github/workflows/e2e-tests.yml` 中不存在 `desktop-e2e` WDIO job。
    - 仓库执行路径中不再有 `wdio`、`run-desktop-e2e.mjs` 活跃调用。
    - `rtk just test-e2e-full` exits 0，并输出两条 lane 的清晰汇总。
  </acceptance_criteria>
  <verify>
    <automated>rtk bun run --cwd e2e-desktop-playwright test:ci && rtk bun run --cwd apps/client/web/app test:e2e && rtk just test-e2e-full</automated>
  </verify>
  <done>仓库 E2E 门禁完成从三轨到双轨收敛，WDIO 旧栈退出执行面。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Align planning evidence with WDIO retirement decision</name>
  <files>.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-VERIFICATION.md, .planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-VALIDATION.md, .planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-CONTEXT.md</files>
  <read_first>
    - .planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-VERIFICATION.md
    - .planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-VALIDATION.md
    - .planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-CONTEXT.md
  </read_first>
  <action>把文档口径更新为“WDIO 已退场”的新现实：移除对 rollback lane 必须 green 的要求，替换为“桌面 tauri-playwright 单栈覆盖必要场景 + 证据可下载 + 全仓门禁可运行”。明确记录该调整为用户决策覆盖旧的 D-06 并行策略，避免后续 agent 继续围绕 WDIO 修复投入。</action>
  <acceptance_criteria>
    - 14-VERIFICATION 不再把 "WDIO suite remains runnable as rollback path" 作为 must-have truth。
    - 14-VALIDATION 的 full suite command 不再包含 `e2e-tests`。
    - 14-CONTEXT 明确记录 WDIO decommission 决策与边界。
  </acceptance_criteria>
  <verify>
    <automated>rtk grep "e2e-tests|WDIO suite remains runnable" .planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs</automated>
  </verify>
  <done>规划层与执行层口径一致，后续验证不再被过时回滚约束阻塞。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrated playwright specs -> desktop runtime | 新迁移用例若断言降级，可能出现假绿 |
| e2e gate command -> merge/release decision | 移除 WDIO 后，若覆盖不完整会导致门禁漏检 |
| CI artifacts -> failure triage | 若 artifact 约定不一致，失败定位时间上升 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | T | migrated admin/agent/tenant specs | mitigate | Preserve assertion semantics and tenant/auth behavior parity with WDIO baseline |
| T-14-14 | R | CI diagnostics after WDIO removal | mitigate | Keep lane-scoped Playwright artifacts (report/test-results/xml/json) with retention policy |
| T-14-15 | D | full-gate command after lane reduction | mitigate | Keep deterministic sequential lane summary and explicit non-zero exit on any lane failure |
</threat_model>

<verification>
- `rtk bun run --cwd e2e-desktop-playwright test:ci`
- `rtk bun run --cwd apps/client/web/app test:e2e`
- `rtk just test-e2e-full`
</verification>

<success_criteria>
- 仓库活动执行路径中不再存在 WDIO 依赖与 `e2e-tests` CI 入口。
- 桌面必要场景（至少 smoke/login/counter/admin/agent/tenant-isolation）由 `e2e-desktop-playwright` 单栈覆盖并可通过。
- QGATE-01 / QGATE-02 的验证证据改由 web + tauri-playwright 双通道持续产出。
</success_criteria>

<output>
After completion, create `.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-05-SUMMARY.md`
</output>
