---
phase: 14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - apps/client/native/src-tauri/capabilities/default.json
  - apps/client/native/src-tauri/capabilities/e2e-testing.json
  - apps/client/native/src-tauri/tauri.e2e.conf.json
  - e2e-desktop-playwright/playwright.config.ts
  - e2e-desktop-playwright/tests/specs/counter.spec.ts
  - e2e-desktop-playwright/tests/specs/login.spec.ts
  - e2e-desktop-playwright/tests/specs/smoke.spec.ts
  - e2e-desktop-playwright/tests/fixtures/tenant.ts
autonomous: true
gap_closure: true
requirements: [QGATE-01]
must_haves:
  truths:
    - "Desktop automation control surface is only available in e2e-testing context"
    - "Maintainer can run migrated desktop smoke/login/counter tests in tauri mode"
    - "Migrated tests preserve existing mock auth and tenant identity semantics in executed specs"
  artifacts:
    - path: "apps/client/native/src-tauri/capabilities/default.json"
      provides: "default profile without playwright permission"
    - path: "apps/client/native/src-tauri/capabilities/e2e-testing.json"
      provides: "e2e-only capability profile containing playwright:default"
    - path: "e2e-desktop-playwright/tests/specs/counter.spec.ts"
      provides: "stable counter assertions using attached-element wait strategy"
  key_links:
    - from: "e2e-desktop-playwright/playwright.config.ts"
      to: "apps/client/native/src-tauri/tauri.e2e.conf.json"
      via: "cargo tauri dev --config tauri.e2e.conf.json --features e2e-testing"
      pattern: "tauri\\.e2e\\.conf\\.json"
    - from: "e2e-desktop-playwright/tests/specs/counter.spec.ts"
      to: "e2e-desktop-playwright/tests/fixtures/tenant.ts"
      via: "import + execution of tenant reset/init helper"
      pattern: "from '../fixtures/tenant'"
---

<objective>
关闭验证缺口中的两条主阻塞：默认构建能力污染与 tauri Phase1 counter 不稳定失败。

Purpose: 按 D-09、D-04、D-10 在不改业务行为前提下恢复默认路径可编译，并让迁移集 smoke/login/counter 稳定可跑。
Output: e2e 独立 capability 配置、tauri e2e 专用配置、稳定的 Phase1 迁移 spec。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-CONTEXT.md
@.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-VERIFICATION.md
@docs/TAURI_PLAYWRIGHT_MIGRATION_CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Isolate playwright capability to e2e-only profile (D-09)</name>
  <files>apps/client/native/src-tauri/capabilities/default.json, apps/client/native/src-tauri/capabilities/e2e-testing.json, apps/client/native/src-tauri/tauri.e2e.conf.json, e2e-desktop-playwright/playwright.config.ts</files>
  <read_first>
    - apps/client/native/src-tauri/capabilities/default.json
    - apps/client/native/src-tauri/tauri.conf.json
    - e2e-desktop-playwright/playwright.config.ts
    - .planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-VERIFICATION.md
  </read_first>
  <action>将 `playwright:default` 从 `capabilities/default.json` 移除；新增 `capabilities/e2e-testing.json`，仅在该文件中声明 `playwright:default` 并绑定主窗口。新增 `tauri.e2e.conf.json` 作为 e2e 覆盖配置，显式加载 `default + e2e-testing` capability 组合。更新 `e2e-desktop-playwright/playwright.config.ts` 的 webServer command 为 `cargo tauri dev --features e2e-testing --config tauri.e2e.conf.json`，确保 e2e 路径可用而默认 `cargo check -p native-tauri` 不再受 playwright capability 影响（per D-09）。</action>
  <acceptance_criteria>
    - default.json no longer contains `playwright:default`.
    - capabilities/e2e-testing.json exists and contains `playwright:default`.
    - tauri.e2e.conf.json exists and references `e2e-testing` capability profile.
    - playwright.config.ts command contains `--features e2e-testing --config tauri.e2e.conf.json`.
  </acceptance_criteria>
  <verify>
    <automated>rtk cargo check -p native-tauri && rtk cargo check -p native-tauri --features e2e-testing</automated>
  </verify>
  <done>默认路径与 e2e 路径都可编译，且自动化控制面仅在 e2e-testing 上下文暴露。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stabilize phase1 specs and consume tenant fixture in migrated execution (D-01, D-02, D-04, D-10)</name>
  <files>e2e-desktop-playwright/tests/specs/counter.spec.ts, e2e-desktop-playwright/tests/specs/login.spec.ts, e2e-desktop-playwright/tests/specs/smoke.spec.ts, e2e-desktop-playwright/tests/fixtures/tenant.ts</files>
  <read_first>
    - e2e-desktop-playwright/tests/specs/counter.spec.ts
    - e2e-desktop-playwright/tests/specs/login.spec.ts
    - e2e-desktop-playwright/tests/specs/smoke.spec.ts
    - e2e-desktop-playwright/tests/fixtures/tenant.ts
    - .planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: `test:phase1` no longer fails with detached-element timeout in counter spec.
    - Test 2: at least one migrated spec imports and executes tenant fixture helpers.
    - Test 3: smoke/login/counter keep auth-guard-compatible assertions without product code changes.
  </behavior>
  <action>重写 counter 关键交互步骤为可重试的 locator/wait 策略：点击前先 `expect(locator).toBeVisible()` + `toBeEnabled()`，每次状态变化后使用文本断言等待稳定。把 `tenant.ts` 中 `initTenantPair/resetTenantPairCounter` 通过显式 import 接入到 migrated specs（优先 counter spec），在进入交互断言前执行 reset，证明租户语义在“执行路径”而不是仅声明。必要时同步收敛 smoke/login 断言等待策略，但禁止修改应用业务逻辑、API 或页面行为（per D-10）。</action>
  <acceptance_criteria>
    - counter.spec.ts no longer uses brittle nth-click flow without visibility/enabled guard.
    - at least one file in tests/specs contains `from '../fixtures/tenant'` and executes exported helper.
    - `rtk bun run --cwd e2e-desktop-playwright test:phase1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk bun run --cwd e2e-desktop-playwright test:phase1</automated>
  </verify>
  <done>Phase1 迁移集稳定通过，tenant fixture 已在迁移用例执行路径被消费。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| default runtime → capability resolver | 默认构建不应依赖 e2e 插件权限 |
| test fixture helpers → auth/tenant semantics | 迁移测试必须复用既有认证和租户语义 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | E | Tauri capability profiles | mitigate | Keep `playwright:default` only in e2e-testing capability profile |
| T-14-11 | D | counter migrated spec | mitigate | Replace detached-element prone flow with explicit visible/enabled waits |
| T-14-12 | T | tenant semantics parity | mitigate | Execute shared tenant fixture helper in migrated spec path (not declaration-only) |
</threat_model>

<verification>
- `rtk cargo check -p native-tauri`
- `rtk cargo check -p native-tauri --features e2e-testing`
- `rtk bun run --cwd e2e-desktop-playwright test:phase1`
</verification>

<success_criteria>
- 14-VERIFICATION.md 中 truth #2/#3/#4 对应缺口具备可执行闭环。
- D-09、D-04、D-10 在实现与验证层面保持一致。
</success_criteria>

<output>
After completion, create `.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-04-SUMMARY.md`
</output>
