---
phase: 14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/native/src-tauri/Cargo.toml
  - apps/client/native/src-tauri/src/lib.rs
  - apps/client/native/src-tauri/capabilities/default.json
  - e2e-desktop-playwright/package.json
  - e2e-desktop-playwright/playwright.config.ts
  - e2e-desktop-playwright/tests/fixtures/tauri.ts
  - e2e-desktop-playwright/tests/specs/smoke.spec.ts
autonomous: true
requirements: [QGATE-01]
must_haves:
  truths:
    - "Maintainer can run a tauri-playwright desktop smoke test without changing product behavior"
    - "Desktop automation control surface is only available in e2e-testing context"
  artifacts:
    - path: "apps/client/native/src-tauri/Cargo.toml"
      provides: "feature-gated tauri-plugin-playwright dependency"
    - path: "apps/client/native/src-tauri/src/lib.rs"
      provides: "conditional plugin registration"
    - path: "e2e-desktop-playwright/playwright.config.ts"
      provides: "independent desktop Playwright tauri-mode config"
  key_links:
    - from: "apps/client/native/src-tauri/src/lib.rs"
      to: "apps/client/native/src-tauri/Cargo.toml"
      via: "cfg(feature = \"e2e-testing\") plugin init"
      pattern: "cfg\(feature = \"e2e-testing\"\)"
    - from: "e2e-desktop-playwright/tests/specs/smoke.spec.ts"
      to: "e2e-desktop-playwright/tests/fixtures/tauri.ts"
      via: "createTauriTest fixture export"
      pattern: "createTauriTest"
---

<objective>
建立 Phase 1 迁移所需的最小可运行底座：Tauri 端 feature-gated tauri-playwright 接入 + 独立桌面 Playwright 新套件脚手架。

Purpose: 先闭合 D-03、D-09、D-10 的基础约束，保证迁移可回滚且不会污染 release 行为。
Output: 可执行的 `e2e-desktop-playwright` smoke 测试与 Tauri 安全门控接入。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-CONTEXT.md
@.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-RESEARCH.md
@docs/TAURI_PLAYWRIGHT_MIGRATION_CONTEXT.md

<interfaces>
From `apps/client/native/src-tauri/src/lib.rs`:
```rust
let mut builder = tauri::Builder::default();
builder = builder.plugin(tauri_plugin_opener::init());
```

From `apps/client/native/src-tauri/capabilities/default.json`:
```json
"permissions": ["core:default", "opener:default", "store:default"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add feature-gated tauri-playwright plugin wiring (D-09, D-10)</name>
  <files>apps/client/native/src-tauri/Cargo.toml, apps/client/native/src-tauri/src/lib.rs, apps/client/native/src-tauri/capabilities/default.json</files>
  <read_first>
    - apps/client/native/src-tauri/Cargo.toml
    - apps/client/native/src-tauri/src/lib.rs
    - apps/client/native/src-tauri/capabilities/default.json
    - docs/TAURI_PLAYWRIGHT_MIGRATION_CONTEXT.md
    - .planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-CONTEXT.md
  </read_first>
  <action>在 `Cargo.toml` 添加可选依赖 `tauri-plugin-playwright`，并定义 feature `e2e-testing = ["dep:tauri-plugin-playwright"]`（per D-09）。在 `src/lib.rs` 中使用 `#[cfg(feature = "e2e-testing")]` 包裹 `.plugin(tauri_plugin_playwright::init())` 注册，确保默认/release 路径不注册该插件（per D-09, D-10）。在 capability `default.json` 增加 `playwright:default` 权限，仅供 e2e feature 路径使用，不改业务命令和现有权限语义（per D-10）。</action>
  <acceptance_criteria>
    - Cargo.toml contains `tauri-plugin-playwright` as `optional = true`.
    - Cargo.toml contains feature key `e2e-testing` referencing `dep:tauri-plugin-playwright`.
    - src/lib.rs contains `cfg(feature = "e2e-testing")` guarded plugin registration.
    - default.json contains `playwright:default` in permissions.
  </acceptance_criteria>
  <verify>
    <automated>rtk cargo check -p native-tauri</automated>
  </verify>
  <done>测试插件已受 feature 门控，默认应用行为与发布语义不变。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create independent desktop Playwright suite scaffold (D-03, D-01)</name>
  <files>e2e-desktop-playwright/package.json, e2e-desktop-playwright/playwright.config.ts, e2e-desktop-playwright/tests/fixtures/tauri.ts, e2e-desktop-playwright/tests/specs/smoke.spec.ts</files>
  <read_first>
    - apps/client/web/app/playwright.config.ts
    - apps/client/web/app/tests/fixtures/auth.ts
    - e2e-tests/wdio.conf.mjs
    - e2e-tests/package.json
    - .planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-CONTEXT.md
  </read_first>
  <action>新建独立根目录 `e2e-desktop-playwright/`（不复用 web/WDIO 测试根，per D-03）。创建 `package.json`（依赖 `@playwright/test` 与 `@srsholmes/tauri-playwright`）与脚本 `test:smoke`、`test:ci`。创建 `playwright.config.ts`，启用 `tauri` project，并设置失败证据策略 `trace: 'on-first-retry'`, `screenshot: 'only-on-failure'`, `video: 'retain-on-failure'`。创建 `tests/fixtures/tauri.ts` 统一导出 `createTauriTest`。新增 `smoke.spec.ts` 验证桌面 runtime 可以启动并访问登录壳层（Phase 1 基础，per D-01），不修改任何产品业务代码（per D-10）。</action>
  <acceptance_criteria>
    - e2e-desktop-playwright exists as standalone suite root.
    - playwright config defines project `tauri` and failure artifact options.
    - smoke spec executes through `createTauriTest` fixture.
    - no files under `e2e-tests/specs` are deleted or moved.
  </acceptance_criteria>
  <verify>
    <automated>rtk bun run --cwd e2e-desktop-playwright test:smoke</automated>
  </verify>
  <done>新桌面套件可独立运行 smoke，用于后续 Phase 1 用例迁移。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test runner → Tauri runtime | 自动化控制面进入桌面应用执行上下文 |
| Build mode → plugin registration | release/default 路径不应暴露测试插件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | E | `src-tauri/src/lib.rs` plugin init | mitigate | Guard plugin registration behind `cfg(feature = "e2e-testing")` only |
| T-14-02 | T | `Cargo.toml` feature wiring | mitigate | Keep plugin optional and excluded from default feature set |
| T-14-03 | I | capability permissions | mitigate | Add only `playwright:default`; avoid expanding unrelated privileged permissions |
</threat_model>

<verification>
- `rtk cargo check -p native-tauri`
- `rtk bun run --cwd e2e-desktop-playwright test:smoke`
</verification>

<success_criteria>
- D-03, D-09, D-10 对应基础约束落地且可自动验证。
- 新桌面套件可运行 smoke，不影响旧 WDIO 可回滚路径。
</success_criteria>

<output>
After completion, create `.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/14-01-SUMMARY.md`
</output>
