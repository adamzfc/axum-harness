---
phase: 14.1-deferred-items-cargo-e2e-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/web/app/tests/fixtures/tenant.ts
  - apps/client/web/app/tests/fixtures/auth.ts
  - apps/client/web/app/tests/e2e/counter.test.ts
  - apps/client/web/app/tests/e2e/tenant-isolation.test.ts
  - apps/client/web/app/tests/fixtures/runtime.ts
autonomous: true
requirements: [QGATE-01]
must_haves:
  truths:
    - "Web E2E tenant bootstrap no longer fails with 401"
    - "Web E2E tenant bootstrap no longer fails with ECONNREFUSED when runtime is absent"
    - "counter and tenant-isolation E2E are reproducibly executable"
  artifacts:
    - path: "apps/client/web/app/tests/fixtures/tenant.ts"
      provides: "tenant init/reset calls include auth contract-compliant headers"
    - path: "apps/client/web/app/tests/fixtures/runtime.ts"
      provides: "shared runtime ready check + owned process lifecycle guard"
    - path: "apps/client/web/app/tests/e2e/tenant-isolation.test.ts"
      provides: "tenant regression consumes deterministic fixture bootstrap path"
  key_links:
    - from: "apps/client/web/app/tests/fixtures/tenant.ts"
      to: "servers/api/src/routes/tenant.rs"
      via: "Authorization header + /api/tenant/init request body"
      pattern: "Authorization|api/tenant/init"
    - from: "apps/client/web/app/tests/e2e/counter.test.ts"
      to: "apps/client/web/app/tests/fixtures/runtime.ts"
      via: "beforeEach runtime readiness"
      pattern: "ensureApiReady|resetTenantPairCounter"
---

<objective>
修复 web Playwright 在 tenant bootstrap 阶段的 401/ECONNREFUSED 阻塞，让关键回归用例进入可重复、可执行状态。

Purpose: 先打通 Phase 14.1 的硬阻塞入口，确保后续全仓 E2E 门禁有真实可用的 web lane 信号。  
Output: 鉴权对齐后的 tenant fixture、共享 runtime readiness helper、已接线的 web 回归用例。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.1-deferred-items-cargo-e2e-e2e/14.1-CONTEXT.md
@.planning/phases/14.1-deferred-items-cargo-e2e-e2e/14.1-RESEARCH.md
@.planning/phases/14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs/deferred-items.md
@servers/api/src/routes/tenant.rs
@e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 对齐 web tenant fixture 与后端鉴权契约（D-01, D-06）</name>
  <files>apps/client/web/app/tests/fixtures/tenant.ts, apps/client/web/app/tests/fixtures/auth.ts</files>
  <read_first>
    - apps/client/web/app/tests/fixtures/tenant.ts
    - apps/client/web/app/tests/fixtures/auth.ts
    - e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts
    - servers/api/src/routes/tenant.rs
  </read_first>
  <action>在 web tenant fixture 中引入与 desktop tenant-isolation 相同语义的测试 token/header 构造：`Authorization: Bearer <test-jwt-like-token>`，并保持请求体字段 `user_sub` / `user_name` 与现有契约一致；不得改动业务后端行为（per D-06），仅修复测试侧请求契约（per D-01）。</action>
  <acceptance_criteria>
    - `apps/client/web/app/tests/fixtures/tenant.ts` 包含 `Authorization` header。
    - `apps/client/web/app/tests/fixtures/tenant.ts` 仍使用 `tenant_a_user`/`tenant_b_user`。
    - 执行 `counter.test.ts` 与 `tenant-isolation.test.ts` 时不再出现 `tenant init failed: status=401`。
  </acceptance_criteria>
  <verify>
    <automated>rtk bun run --cwd apps/client/web/app test:e2e --project=desktop-chrome tests/e2e/counter.test.ts tests/e2e/tenant-isolation.test.ts</automated>
  </verify>
  <done>tenant fixture 对后端鉴权契约完整对齐，401 阻塞消失。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 抽取并接线 web E2E runtime readiness helper（D-02, D-04）</name>
  <files>apps/client/web/app/tests/fixtures/runtime.ts, apps/client/web/app/tests/e2e/counter.test.ts, apps/client/web/app/tests/e2e/tenant-isolation.test.ts</files>
  <read_first>
    - apps/client/web/app/tests/e2e/counter.test.ts
    - apps/client/web/app/tests/e2e/tenant-isolation.test.ts
    - apps/client/web/app/tests/fixtures/tenant.ts
    - e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts
  </read_first>
  <action>新增 `runtime.ts`（test-only）封装 API `/readyz` 轮询、按需启动 runtime_server、owned process 清理守卫，并在 web 关键用例 beforeEach/beforeAll 中接入该 helper（per D-02）；同时加入 deterministic 失败信息（端口/ready 超时）而非隐式重试（per D-04）。</action>
  <acceptance_criteria>
    - `apps/client/web/app/tests/fixtures/runtime.ts` 提供显式 `ensureApiReady` 能力。
    - `counter.test.ts` 与 `tenant-isolation.test.ts` 引用 runtime helper。
    - 目标回归命令不再报 `ECONNREFUSED 127.0.0.1:3001`。
  </acceptance_criteria>
  <verify>
    <automated>rtk bun run --cwd apps/client/web/app test:e2e --project=desktop-chrome tests/e2e/counter.test.ts tests/e2e/tenant-isolation.test.ts</automated>
  </verify>
  <done>web E2E 在 runtime_server 冷/热状态下均可稳定进入测试执行阶段。</done>
</task>

<task type="auto">
  <name>Task 3: 执行 web 全量回归确认 lane 恢复（D-07）</name>
  <files>apps/client/web/app/tests/e2e/counter.test.ts, apps/client/web/app/tests/e2e/tenant-isolation.test.ts</files>
  <read_first>
    - apps/client/web/app/playwright.config.ts
    - apps/client/web/app/package.json
    - apps/client/web/app/tests/e2e/counter.test.ts
    - apps/client/web/app/tests/e2e/tenant-isolation.test.ts
  </read_first>
  <action>在契约与启动修复后，执行 web lane 全量 E2E，记录是否全绿与失败证据路径，确保本计划输出的是可复现、可诊断的 lane 恢复结果（per D-07）。</action>
  <acceptance_criteria>
    - `rtk bun run --cwd apps/client/web/app test:e2e` exits 0。
    - 失败场景（若存在）有可定位输出目录（playwright-report/test-results）。
  </acceptance_criteria>
  <verify>
    <automated>rtk bun run --cwd apps/client/web/app test:e2e</automated>
  </verify>
  <done>web E2E lane 恢复为可全量执行、可判断绿/红的状态。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web fixture → tenant init API | 测试输入跨越到鉴权接口，未经授权会触发 401 |
| runtime helper → local process | 启动/复用进程的边界处理错误会导致假红或泄漏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-01 | S | web tenant fixture auth header | mitigate | 在 fixture 中显式构造并附带 Bearer token，拒绝匿名 tenant init 调用 |
| T-14.1-02 | D | runtime readiness orchestration | mitigate | 统一 readyz 轮询、超时、owned process 清理，避免反复启动与端口竞争 |
| T-14.1-03 | R | e2e diagnostics traceability | mitigate | 保持 playwright-report/test-results 证据目录，命令失败时输出定位信息 |
</threat_model>

<verification>
- `rtk bun run --cwd apps/client/web/app test:e2e --project=desktop-chrome tests/e2e/counter.test.ts tests/e2e/tenant-isolation.test.ts`
- `rtk bun run --cwd apps/client/web/app test:e2e`
</verification>

<success_criteria>
- web fixture 不再触发 tenant init 401。
- web 关键回归用例不再因 runtime 未就绪而 ECONNREFUSED。
- web 全量 E2E 可执行且结果可诊断。
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-deferred-items-cargo-e2e-e2e/14.1-01-SUMMARY.md`
</output>
