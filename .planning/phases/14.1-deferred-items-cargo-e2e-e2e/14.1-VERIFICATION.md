---
phase: 14.1
slug: deferred-items-cargo-e2e-e2e
status: active
updated: 2026-04-07
---

# Phase 14.1 — Verification Contract

## Status

- **Execution state:** Ready for verifier handoff
- **Gate command (repo-level):** `just test-e2e-full`
- **Active lanes:** `web-e2e`, `desktop-e2e-playwright-tauri`

## Observable Truths

1. **Web + Tauri 双 lane 仍是唯一活动门禁通道**  
   Evidence: `.github/workflows/e2e-tests.yml` 仅定义 `web-e2e` 与 `desktop-e2e-playwright-tauri` 两条执行 lane。

2. **full gate 在跑 lane 前执行 deterministic preflight**  
   Evidence: `moon.yml` `test-e2e-full` 先执行 `scripts/e2e/runtime-preflight.mjs`，失败即 fail-fast 并输出 summary。

3. **CI 证据归档覆盖双 lane 且 retention 固化**  
   Evidence: 两个 `actions/upload-artifact@v4` 分别采集 web 与 tauri 报告/结果目录，并统一 `retention-days: 7`。

4. **summary 显式展示 lane 粒度状态**  
   Evidence: `summary` job 输出 `web-e2e` 与 `desktop-e2e-playwright-tauri` 两行状态表，供审计与放行判断。

5. **验证与执行命令已统一到仓库级入口**  
   Evidence: `14.1-VALIDATION.md` 与本文件都以 `just test-e2e-full` 作为 full gate 命令，避免本地/CI 语义漂移。

6. **Phase 14.1 roadmap 已具备可执行计划清单**  
   Evidence: `.planning/ROADMAP.md` 中 Phase 14.1 显示 `Plans: 3 plans` 并列出 `14.1-01/02/03-PLAN.md`。

## Key Artifacts

- `.github/workflows/e2e-tests.yml`
- `moon.yml`
- `.planning/phases/14.1-deferred-items-cargo-e2e-e2e/14.1-VALIDATION.md`
- `.planning/ROADMAP.md`

## Must-Have Truth Checks

| Must-have | Check command | Expected |
|-----------|---------------|----------|
| CI 与本地 gate 约定一致 | `just test-e2e-full` | preflight-first, then web+tauri lane summary |
| CI lane/证据可审计 | `rtk grep "web-e2e|desktop-e2e-playwright-tauri|upload-artifact|retention-days" .github/workflows/e2e-tests.yml` | 两 lane + artifact + retention 命中 |
| 验证契约文档可执行 | `rtk grep "nyquist_compliant|test-e2e-full|Observable Truths|Status" .planning/phases/14.1-deferred-items-cargo-e2e-e2e/14.1-VALIDATION.md .planning/phases/14.1-deferred-items-cargo-e2e-e2e/14.1-VERIFICATION.md` | 文档包含 gate 命令与 truths |

## Remaining Manual Verification

- 检查受保护分支 required checks 是否仍绑定 active E2E checks（GitHub 分支保护设置，CLI 不直接覆盖）。
