---
phase: 03-runtime-boundary-convergence
plan: '04'
type: execute
wave: 3
depends_on:
  - '03-01'
  - '03-02'
  - '03-03'
files_modified:
  - deny.toml
  - moon.yml
  - .agents/rubrics/boundary-compliance.md
autonomous: true
requirements:
  - RUNTIME-03

must_haves:
  truths:
    - "deny.toml exists with dependency direction rules preventing core from depending on adapters"
    - "moon.yml has a repo:boundary-check task that verifies dependency directions"
    - ".agents/rubrics/boundary-compliance.md exists with semantic boundary rules"
    - "cargo check --workspace passes with deny.toml present"
  artifacts:
    - path: "deny.toml"
      provides: "cargo-deny configuration with dependency direction rules"
      contains: "[dependencies]"
    - path: "moon.yml"
      provides: "Extended verify pipeline with boundary check task"
      contains: "repo:boundary-check"
    - path: ".agents/rubrics/boundary-compliance.md"
      provides: "Agent-readable boundary compliance rubric"
      min_lines: 30
  key_links:
    - from: "moon.yml repo:verify"
      to: "repo:boundary-check"
      via: "deps"
      pattern: "repo:boundary-check"
    - from: ".agents/rubrics/boundary-compliance.md"
      to: "agent code review"
      via: "semantic rules"
      pattern: "adapter.*no.*business"
---

<objective>
Establish boundary enforcement mechanisms: cargo-deny rules, CI check task, and agent rubric.

Purpose: Without automated enforcement, boundary violations will creep back in. Phase 3 needs hard CI gates (cargo-deny) and soft agent gates (rubric) to maintain the core/adapters/hosts separation.
Output: deny.toml with dependency direction rules, moon.yml with boundary-check task, .agents/rubrics/boundary-compliance.md with semantic rules.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-runtime-boundary-convergence/03-CONTEXT.md
@.planning/phases/03-runtime-boundary-convergence/03-RESEARCH.md

# Design decisions from context (MANDATORY)
- D-13: CI hard check: repo:verify increases with boundary check task, verifying dependency direction (core cannot depend on adapters/hosts, contracts cannot depend on core/adapters)
- D-14: cargo-deny config with [dependencies] rules limiting illegal crate dependency direction
- D-15: .agents/rubrics/boundary-compliance.md writes semantic boundary spec (adapter has no business rules, usecases doesn't depend on host)
- D-16: Agent code review checks rubric, CI does hard gating

# Boundary Rules (from research)
| Boundary | Rule |
|----------|------|
| domain must NOT depend on | adapters, hosts, contracts_api, servers |
| usecases must NOT depend on | adapters, hosts, contracts_api, servers |
| contracts_api must NOT depend on | domain, usecases, adapters, hosts |
| adapters/storage/* must depend on | domain (port traits) — OK |
| adapters/hosts/* must depend on | domain, usecases — OK |

# Current moon.yml structure
moon.yml has tasks: repo:setup, repo:dev-fullstack, repo:typegen, repo:contracts-check, repo:verify
repo:verify currently depends on: repo:contracts-check
Need to add: repo:boundary-check as another dependency of repo:verify
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deny.toml with dependency direction rules</name>
  <files>
    - deny.toml
  </files>
  <read_first>
    - Cargo.toml (workspace members and deps to understand current structure)
  </read_first>
  <action>
Create `deny.toml` at project root:

```toml
# cargo-deny configuration
# Docs: https://embarkstudios.github.io/cargo-deny/

[graph]
# Check all workspace members
all-features = true

[advisories]
# Disable advisory check for now (can enable later)
version = 2

[licenses]
# Disable license check for now (can enable later)
unlicensed = "deny"

[bans]
# Deny specific dependency directions to enforce hexagonal architecture
# These rules prevent core crates from depending on host/adapter crates

# Rule: domain must not depend on adapters, hosts, contracts, or servers
[[bans.deny]]
name = "storage_surrealdb"
wrappers = ["domain", "usecases"]

[[bans.deny]]
name = "storage_libsql"
wrappers = ["domain", "usecases"]

[[bans.deny]]
name = "runtime_tauri"
wrappers = ["domain", "usecases", "contracts_api"]

[[bans.deny]]
name = "contracts_api"
wrappers = ["domain", "usecases"]

[[bans.deny]]
name = "runtime_server"
wrappers = ["domain", "usecases", "contracts_api", "storage_surrealdb", "storage_libsql", "runtime_tauri"]

# Rule: contracts must not depend on domain, usecases, adapters, or hosts
[[bans.deny]]
name = "domain"
wrappers = ["contracts_api", "contracts_auth", "contracts_events"]

[[bans.deny]]
name = "usecases"
wrappers = ["contracts_api", "contracts_auth", "contracts_events"]
```

Note: The `wrappers` field lists crates that are NOT allowed to depend on the denied crate. This effectively enforces dependency direction.
  </action>
  <acceptance_criteria>
    - deny.toml exists at project root
    - Contains [bans.deny] rules preventing domain/usecases from depending on adapters/hosts
    - Contains rules preventing contracts from depending on domain/usecases
    - cargo check --workspace passes with deny.toml present
  </acceptance_criteria>
  <verify>
    <automated>test -f deny.toml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>deny.toml created with dependency direction enforcement rules</done>
</task>

<task type="auto">
  <name>Task 2: Add boundary-check task to moon.yml and wire into verify</name>
  <files>
    - moon.yml
  </files>
  <read_first>
    - moon.yml (current task graph)
    - deny.toml (rules to check)
  </read_first>
  <action>
Edit `moon.yml` to add the `repo:boundary-check` task and wire it into `repo:verify`:

1. Add new task `repo:boundary-check`:
```yaml
  repo:boundary-check:
    command:
      - 'echo "=== Checking domain dependencies ==="'
      - 'cargo tree -p domain --depth 1 2>/dev/null | grep -v "domain" | grep -v "^\s*$" | grep -v "async-trait" | grep -v "serde" | grep -v "serde_json" | grep -E "(storage_|runtime_|contracts_)" && echo "FAIL: domain depends on illegal crate" && exit 1 || echo "OK: domain boundary clean"'
      - 'echo "=== Checking usecases dependencies ==="'
      - 'cargo tree -p usecases --depth 1 2>/dev/null | grep -v "usecases" | grep -v "^\s*$" | grep -v "async-trait" | grep -v "serde" | grep -v "serde_json" | grep -v "chrono" | grep -v "thiserror" | grep -E "(storage_|runtime_|contracts_)" && echo "FAIL: usecases depends on illegal crate" && exit 1 || echo "OK: usecases boundary clean"'
      - 'echo "=== Checking contracts_api dependencies ==="'
      - 'cargo tree -p contracts_api --depth 1 2>/dev/null | grep -v "contracts_api" | grep -v "^\s*$" | grep -v "serde" | grep -v "ts-rs" | grep -v "utoipa" | grep -v "validator" | grep -E "(domain|usecases|storage_|runtime_)" && echo "FAIL: contracts_api depends on illegal crate" && exit 1 || echo "OK: contracts_api boundary clean"'
      - 'echo "All boundary checks passed"'
    inputs:
      - "Cargo.toml"
      - "packages/*/Cargo.toml"
      - "packages/**/Cargo.toml"
      - "servers/*/Cargo.toml"
    deps:
      - "~:repo-setup"
    options:
      runFromWorkspaceRoot: true
```

2. Update `repo:verify` task to add `repo:boundary-check` as a dependency:
   - Add `- "~:repo:boundary-check"` to the `deps` array of `repo:verify`
  </action>
  <acceptance_criteria>
    - moon.yml contains repo:boundary-check task
    - repo:boundary-check checks cargo tree for domain, usecases, contracts_api
    - repo:verify has repo:boundary-check in its deps
  </acceptance_criteria>
  <verify>
    <automated>grep -q "repo:boundary-check" moon.yml && grep -q "boundary-check" moon.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>moon.yml has boundary check task wired into verify pipeline</done>
</task>

<task type="auto">
  <name>Task 3: Create agent boundary compliance rubric</name>
  <files>
    - .agents/rubrics/boundary-compliance.md
  </files>
  <read_first>
    - .planning/phases/03-runtime-boundary-convergence/03-CONTEXT.md §边界强制机制
  </read_first>
  <action>
Create `.agents/rubrics/boundary-compliance.md`:

```markdown
# Boundary Compliance Rubric

**Purpose:** Agent code review checklist for hexagonal architecture boundary compliance.
**Enforced by:** CI (repo:boundary-check) + agent review (this rubric).

## Layer Rules

### domain (packages/core/domain/)
- MUST NOT import from: adapters/*, hosts/*, contracts_*, servers/*
- MUST NOT reference: tauri, axum, surrealdb, libsql, reqwest
- CAN import: async-trait, serde, serde_json, std
- Contains: Port traits (SurrealDbPort, LibSqlPort), value objects (TenantId), error types

### usecases (packages/core/usecases/)
- MUST NOT import from: adapters/*, hosts/*, contracts_*, servers/*
- MUST NOT reference: tauri, axum
- CAN import: domain, async-trait, serde, serde_json, chrono, thiserror
- Contains: Service traits, service implementations, internal DTOs
- Rule: usecases defines its OWN input/output types (e.g., CreateTenantInput), NOT contracts_api types

### contracts_* (packages/contracts/api, auth, events)
- MUST NOT import from: domain, usecases, adapters/*, hosts/*, servers/*
- CAN import: serde, ts-rs, utoipa, validator
- Contains: Cross-boundary DTOs only, no business logic

### adapters/storage/* (packages/adapters/storage/surrealdb, libsql)
- CAN import: domain (for port traits)
- MUST NOT import from: usecases, contracts_*, servers/*
- MUST NOT contain: business rules, query logic beyond port trait implementation
- Contains: Port trait implementations, migration scripts

### adapters/hosts/* (packages/adapters/hosts/tauri)
- CAN import: domain, usecases
- MUST NOT import from: contracts_* (unless feature crates bridge)
- Contains: Tauri command handlers that bridge to usecases
- Rule: command handlers delegate to usecases, no inline business logic

### servers/* (servers/api)
- CAN import: domain, usecases, contracts_api, adapters/*
- Contains: HTTP route handlers, Axum state, middleware
- Rule: route handler maps contracts_api DTO ↔ usecases types

## Review Checklist

When reviewing code changes, verify:
- [ ] No new cross-layer imports violating rules above
- [ ] New adapter implementations reference domain port traits, not concrete types
- [ ] usecases code does NOT import from contracts_api
- [ ] Command handlers in runtime_tauri delegate to usecases
- [ ] No business logic in servers/api route handlers (only mapping + delegation)
```

Create `.agents/rubrics/` directory if it doesn't exist.
  </action>
  <acceptance_criteria>
    - .agents/rubrics/boundary-compliance.md exists
    - Contains layer rules for domain, usecases, contracts, adapters, hosts, servers
    - Contains review checklist
    - Minimum 30 lines
  </acceptance_criteria>
  <verify>
    <automated>test -f .agents/rubrics/boundary-compliance.md && wc -l < .agents/rubrics/boundary-compliance.md | grep -E "[3-9][0-9]|[0-9]{3}" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Agent boundary compliance rubric created</done>
</task>

</tasks>

<verification>
- [ ] deny.toml exists with dependency direction rules
- [ ] moon.yml has repo:boundary-check task
- [ ] repo:verify depends on repo:boundary-check
- [ ] .agents/rubrics/boundary-compliance.md exists with layer rules
- [ ] cargo check --workspace passes
</verification>

<success_criteria>
Three enforcement mechanisms in place: cargo-deny hard rules, CI boundary check task, agent rubric for code review. Boundary violations will be caught by CI before merge and by agent review during development.
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-boundary-convergence/03-04-SUMMARY.md`
</output>
