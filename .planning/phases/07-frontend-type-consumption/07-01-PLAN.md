---
phase: 07-frontend-type-consumption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/api/src/lib.rs
  - packages/contracts/auth/src/lib.rs
  - apps/client/web/app/src/lib/generated/auth/TokenPair.ts
  - apps/client/web/app/src/lib/generated/auth/OAuthCallback.ts
  - apps/client/web/app/src/lib/generated/auth/UserSession.ts
  - apps/client/web/app/src/lib/generated/api/ChatMessage.ts
  - apps/client/web/app/src/lib/generated/api/ToolCall.ts
  - apps/client/web/app/src/lib/generated/api/AgentConfig.ts
  - apps/client/web/app/src/lib/generated/api/HealthResponse.ts
  - apps/client/web/app/src/lib/generated/api/InitTenantRequest.ts
  - apps/client/web/app/src/lib/generated/api/InitTenantResponse.ts
  - apps/client/web/app/src/lib/generated/events/TenantCreated.ts
  - apps/client/web/app/src/lib/generated/events/TenantMemberAdded.ts
autonomous: true
requirements:
  - CONTRACT-02
  - CONTRACT-01

must_haves:
  truths:
    - "所有 contracts 中的类型都能生成到 frontend/generated/"
    - "生成的类型文件存在且可导入"
    - "moon run repo:typegen 后所有类型同步到前端"
  artifacts:
    - path: "apps/client/web/app/src/lib/generated/auth/TokenPair.ts"
      provides: "TokenPair 类型定义"
      exports: ["TokenPair"]
    - path: "apps/client/web/app/src/lib/generated/auth/UserSession.ts"
      provides: "UserSession 类型定义"
      exports: ["UserSession"]
    - path: "apps/client/web/app/src/lib/generated/api/ChatMessage.ts"
      provides: "ChatMessage 类型定义"
      exports: ["ChatMessage"]
    - path: "apps/client/web/app/src/lib/generated/api/ToolCall.ts"
      provides: "ToolCall 类型定义"
      exports: ["ToolCall"]
    - path: "apps/client/web/app/src/lib/generated/api/AgentConfig.ts"
      provides: "AgentConfig 类型定义"
      exports: ["AgentConfig"]
  key_links:
    - from: "packages/contracts/api/src/lib.rs"
      to: "apps/client/web/app/src/lib/generated/api/"
      via: "ts-rs export + moon repo:typegen"
      pattern: "ts\\(export"
    - from: "packages/contracts/auth/src/lib.rs"
      to: "apps/client/web/app/src/lib/generated/auth/"
      via: "ts-rs export + moon repo:typegen"
      pattern: "ts\\(export"
---

<objective>
修复 typegen 管道确保所有 contracts 类型生成并同步到前端 generated/ 目录。

Purpose: 当前 contracts 中定义的 ChatMessage、ToolCall、AgentConfig 等类型未生成到前端，
导致前端无法导入这些类型。需要先确保生成管道完整，才能进行后续的类型消费。

Output: 完整的 generated/ 目录，包含所有 contracts 中定义的类型。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-contracts-typegen/02-01-SUMMARY.md
@.planning/phases/02-contracts-typegen/02-02-SUMMARY.md

# 当前生成状态
@packages/contracts/auth/src/lib.rs
@packages/contracts/api/src/lib.rs

# 现有 generated 文件
@apps/client/web/app/src/lib/generated/auth/TokenPair.ts
@apps/client/web/app/src/lib/generated/auth/UserSession.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行 typegen 并验证所有类型已生成</name>
  <files>packages/contracts/generated/, apps/client/web/app/src/lib/generated/</files>
  <action>
    1. 运行 `moon run repo:typegen` 触发完整的类型生成流程
    2. 检查 packages/contracts/generated/ 下是否有 api/、auth/、events/ 三个子目录
    3. 检查 apps/client/web/app/src/lib/generated/ 下是否有对应的 .ts 文件
    4. 特别验证以下文件是否存在：
       - auth/TokenPair.ts, auth/OAuthCallback.ts, auth/UserSession.ts
       - api/HealthResponse.ts, api/InitTenantRequest.ts, api/InitTenantResponse.ts
       - api/ChatMessage.ts, api/ToolCall.ts, api/AgentConfig.ts
       - events/TenantCreated.ts, events/TenantMemberAdded.ts
    5. 如果缺失任何文件，检查 contracts 中对应 struct 的 #[ts(export)] 注解是否正确
  </action>
  <verify>
    <automated>ls apps/client/web/app/src/lib/generated/auth/TokenPair.ts apps/client/web/app/src/lib/generated/api/ChatMessage.ts apps/client/web/app/src/lib/generated/api/AgentConfig.ts</automated>
  </verify>
  <done>所有 11 个 generated 类型文件存在于 frontend/src/lib/generated/ 目录</done>
</task>

<task type="auto">
  <name>Task 2: 修复 ts-rs bigint 类型问题</name>
  <files>apps/client/web/app/src/lib/generated/auth/TokenPair.ts</files>
  <action>
    TokenPair 中的 expires_in 字段被生成为 `bigint`（Rust i64 → TS bigint），
    但 TypeScript 中 bigint 不能直接用于 JSON 序列化。
    
    检查生成的 TokenPair.ts 中 expires_in 的类型：
    - 如果是 `bigint`，需要修改 contracts/auth/src/lib.rs 中 TokenPair 的 ts-rs 注解
    - 使用 #[ts(type = "number")] 将 expires_in 导出为 number 类型
    - 重新运行 moon run repo:typegen
    
    同时检查其他生成文件中是否有类似的 bigint 问题。
  </action>
  <verify>
    <automated>grep -r "bigint" apps/client/web/app/src/lib/generated/ || echo "NO_BIGINT_ISSUES"</automated>
  </verify>
  <done>所有 generated 类型文件中无 bigint 类型（全部使用 number）</done>
</task>

<task type="auto">
  <name>Task 3: 运行 drift check 验证类型同步一致性</name>
  <files>packages/contracts/generated/, apps/client/web/app/src/lib/generated/</files>
  <action>
    1. 运行 `moon run repo:contracts-check` 执行完整的 drift check
    2. 验证 git diff --exit-code 对 packages/contracts/generated/ 和 apps/client/web/app/src/lib/generated/ 都通过
    3. 如果 drift check 失败，说明生成类型与提交版本不一致，需要重新生成并提交
  </action>
  <verify>
    <automated>moon run repo:contracts-check</automated>
  </verify>
  <done>repo:contracts-check 通过，无 drift，frontend 类型与 contracts 同步</done>
</task>

</tasks>

<verification>
- [ ] moon run repo:typegen 成功执行
- [ ] 所有 11 个类型文件存在于 generated/ 目录
- [ ] 无 bigint 类型问题
- [ ] repo:contracts-check 通过
</verification>

<success_criteria>
- 所有 contracts 中定义的类型都能在 frontend/src/lib/generated/ 中找到对应文件
- 生成管道工作正常，修改 contracts 后重新运行 typegen 即可同步
- 类型文件中无 TypeScript 兼容性问题（如 bigint）
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-type-consumption/07-01-SUMMARY.md`
</output>
