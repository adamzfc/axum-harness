---
phase: 07-frontend-type-consumption
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
files_modified:
  - apps/client/web/app/src/routes/(app)/agent/+page.svelte
  - apps/client/web/app/src/routes/(app)/admin/+page.svelte
autonomous: true
requirements:
  - CONTRACT-02

must_haves:
  truths:
    - "agent/+page.svelte 使用 generated ChatMessage、ToolCall、AgentConfig 类型"
    - "admin/+page.svelte 使用 generated 类型替代内联 stats 对象"
    - "不再存在与 contracts 重复的 inline 类型定义"
  artifacts:
    - path: "apps/client/web/app/src/routes/(app)/agent/+page.svelte"
      provides: "Agent 页面，使用 generated 类型"
      imports: ["ChatMessage", "ToolCall", "AgentConfig"]
    - path: "apps/client/web/app/src/routes/(app)/admin/+page.svelte"
      provides: "Admin 页面，使用 generated 类型"
      imports: ["HealthResponse"]
  key_links:
    - from: "apps/client/web/app/src/routes/(app)/agent/+page.svelte"
      to: "apps/client/web/app/src/lib/generated/api/"
      via: "import from generated paths"
      pattern: "from '\\$lib/generated/api/"
    - from: "apps/client/web/app/src/routes/(app)/admin/+page.svelte"
      to: "apps/client/web/app/src/lib/generated/api/"
      via: "import from generated paths"
      pattern: "from '\\$lib/generated/api/"
---

<objective>
将 agent 和 admin 页面中的 inline 类型替换为 typegen 生成的类型。

Purpose: agent/+page.svelte 中定义了 Conversation、Message、SettingsConfig 等 inline 类型，
其中 Message 和 SettingsConfig 与 contracts 中的 ChatMessage、AgentConfig 语义重叠。
admin/+page.svelte 使用内联 stats 对象，应使用生成的类型。

Output: agent 和 admin 页面使用 generated 类型，消除重复定义。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-frontend-type-consumption/07-01-SUMMARY.md

# 当前 inline 类型定义（需要被替换）
@apps/client/web/app/src/routes/(app)/agent/+page.svelte
@apps/client/web/app/src/routes/(app)/admin/+page.svelte

# Generated 类型（替换源）
@apps/client/web/app/src/lib/generated/api/ChatMessage.ts
@apps/client/web/app/src/lib/generated/api/ToolCall.ts
@apps/client/web/app/src/lib/generated/api/AgentConfig.ts
@apps/client/web/app/src/lib/generated/api/HealthResponse.ts
</context>

<interfaces>
<!-- Generated types from contracts/api -->

From apps/client/web/app/src/lib/generated/api/ChatMessage.ts:
```typescript
export type ChatMessage = {
  id: string,
  conversation_id: string,
  role: string,  // "user" | "assistant" | "system" | "tool"
  content: string,
  tool_calls: Option<Vec<ToolCall>>,  // will be ToolCall[] | null in TS
  created_at: string,
};
```

From apps/client/web/app/src/lib/generated/api/ToolCall.ts:
```typescript
export type ToolCall = {
  id: string,
  name: string,
  arguments: any,  // serde_json::Value
  result: Option<string>,  // string | null
};
```

From apps/client/web/app/src/lib/generated/api/AgentConfig.ts:
```typescript
export type AgentConfig = {
  api_key: string,
  base_url: string,
  model: string,
};
```

Current inline types in agent/+page.svelte to be replaced:
```typescript
// Message - maps to ChatMessage (partial, missing tool_calls, created_at)
type Message = {
  id: string;
  role: 'user' | 'assistant' | 'system' | 'tool' | string;
  content: string;
};

// SettingsConfig - maps to AgentConfig (different field naming)
type SettingsConfig = {
  apiKey: string;
  baseUrl: string;
  model: string;
};

// Conversation - NOT in contracts, keep as-is (UI-only concept)
type Conversation = {
  id: string;
  title: string;
  created_at?: string;
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 替换 agent/+page.svelte 中的 inline 类型为 generated 类型</name>
  <files>apps/client/web/app/src/routes/(app)/agent/+page.svelte</files>
  <action>
    修改 apps/client/web/app/src/routes/(app)/agent/+page.svelte：
    
    1. 删除 inline 的 Message 和 SettingsConfig 类型定义
    2. 从 generated 导入类型：
       ```typescript
       import type { ChatMessage } from '$lib/generated/api/ChatMessage';
       import type { AgentConfig } from '$lib/generated/api/AgentConfig';
       ```
    3. 将 Message 类型的使用替换为 ChatMessage：
       - messages 状态类型改为 `ChatMessage[]`
       - 注意 ChatMessage 有 tool_calls 和 created_at 字段，前端代码需要适配
       - 临时消息（temp-user, temp-assistant）需要构造符合 ChatMessage 结构的对象
    4. 将 SettingsConfig 替换为 AgentConfig：
       - 注意字段命名差异：AgentConfig 使用 snake_case (api_key, base_url)
       - loadSettings 返回类型改为 AgentConfig
       - 更新字段访问：config.apiKey → config.api_key
    5. 保留 Conversation 类型（这是 UI 概念，contracts 中暂无对应）
    6. 更新 sendMessage 中的请求体构造，使用 AgentConfig 字段名
  </action>
  <verify>
    <automated>grep -c "type Message\|type SettingsConfig" apps/client/web/app/src/routes/\(app\)/agent/+page.svelte | grep "^0$"</automated>
  </verify>
  <done>agent/+page.svelte 使用 ChatMessage 和 AgentConfig，不再定义重复类型</done>
</task>

<task type="auto">
  <name>Task 2: 在 contracts/api 中添加 AdminDashboardStats 类型</name>
  <files>packages/contracts/api/src/lib.rs, apps/client/web/app/src/lib/generated/api/AdminDashboardStats.ts</files>
  <action>
    admin/+page.svelte 使用内联 stats 对象 { tenant_count, counter_value, last_login, app_version }，
    但 contracts 中无对应类型。
    
    在 packages/contracts/api/src/lib.rs 中添加：
    ```rust
    /// Admin dashboard statistics.
    #[derive(Debug, Clone, Serialize, Deserialize, ToSchema, TS)]
    #[ts(export, export_to = "api/")]
    pub struct AdminDashboardStats {
        pub tenant_count: u64,
        pub counter_value: i64,
        pub last_login: Option<String>,
        pub app_version: String,
    }
    ```
    
    添加对应测试：
    ```rust
    #[test]
    fn export_admin_dashboard_stats() {
        AdminDashboardStats::export().unwrap();
    }
    ```
    
    运行 moon run repo:typegen 生成 TypeScript 类型。
  </action>
  <verify>
    <automated>ls apps/client/web/app/src/lib/generated/api/AdminDashboardStats.ts && grep "export type AdminDashboardStats" apps/client/web/app/src/lib/generated/api/AdminDashboardStats.ts</automated>
  </verify>
  <done>AdminDashboardStats 类型从 contracts 生成到 frontend/generated/api/，可导入使用</done>
</task>

<task type="auto">
  <name>Task 3: 替换 admin/+page.svelte 中的 inline stats 类型为 generated 类型</name>
  <files>apps/client/web/app/src/routes/(app)/admin/+page.svelte</files>
  <action>
    修改 apps/client/web/app/src/routes/(app)/admin/+page.svelte：
    
    1. 从 generated 导入 AdminDashboardStats：
       ```typescript
       import type { AdminDashboardStats } from '$lib/generated/api/AdminDashboardStats';
       ```
    2. 将 stats 状态类型从内联对象改为 AdminDashboardStats：
       ```typescript
       let stats = $state<AdminDashboardStats>({
         tenant_count: 0,
         counter_value: 0,
         last_login: null,
         app_version: '0.0.0'
       });
       ```
    3. 注意：AdminDashboardStats 中 last_login 是 Option<String>，
       TypeScript 中为 `string | null`，与现有代码兼容
    4. 验证 statCards 的 $derived 表达式与新类型兼容
  </action>
  <verify>
    <automated>grep "import type { AdminDashboardStats }" apps/client/web/app/src/routes/\(app\)/admin/+page.svelte</automated>
  </verify>
  <done>admin/+page.svelte 使用 AdminDashboardStats 类型，stats 对象有明确类型定义</done>
</task>

</tasks>

<verification>
- [ ] agent/+page.svelte 导入 ChatMessage 和 AgentConfig
- [ ] agent/+page.svelte 不再定义 Message 或 SettingsConfig
- [ ] admin/+page.svelte 导入 AdminDashboardStats
- [ ] admin/+page.svelte 使用 AdminDashboardStats 类型定义 stats
- [ ] TypeScript 类型检查通过
- [ ] rg "from '\$lib/generated/" apps/client/web/app/src/routes/\(app\)/agent/+page.svelte 有匹配
- [ ] rg "from '\$lib/generated/" apps/client/web/app/src/routes/\(app\)/admin/+page.svelte 有匹配
</verification>

<success_criteria>
- agent 和 admin 页面使用 generated 类型
- 不再有与 contracts 语义重叠的 inline 类型定义
- TypeScript 编译无错误
- 页面功能不受影响（类型替换不改变运行时行为）
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-type-consumption/07-03-SUMMARY.md`
</output>
