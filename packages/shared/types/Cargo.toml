[package]
name = "shared-types"
version = "0.1.0"
edition = "2024"
description = "Cross-cutting types — Result/Either, Paginated, Timestamped, ID types"

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
