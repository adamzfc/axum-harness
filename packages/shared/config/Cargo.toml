[package]
name = "shared-config"
version = "0.1.0"
edition = "2024"
description = "Environment detection, feature flags, API endpoints, runtime configuration"

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
