[package]
name = "shared-testing"
version = "0.1.0"
edition = "2024"
description = "Shared test utilities — fixtures, mocks, builders"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
