[package]
name = "adapter-hosts-browser-extension"
version = "0.1.0"
edition = "2024"
description = "Chrome/Firefox extension API bridge — storage, tabs, messaging"

[dependencies]

[lints]
workspace = true
