[package]
name = "adapter-hosts-base-app"
version = "0.1.0"
edition = "2024"
description = "Coinbase Base App SDK bridge — wallet connect, identity, notifications"

[dependencies]

[lints]
workspace = true
