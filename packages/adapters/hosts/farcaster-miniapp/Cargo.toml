[package]
name = "adapter-hosts-farcaster-miniapp"
version = "0.1.0"
edition = "2024"
description = "Farcaster Frame SDK bridge — context, actions, signer, quickAuth"

[dependencies]

[lints]
workspace = true
