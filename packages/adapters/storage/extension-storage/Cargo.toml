[package]
name = "adapter-storage-extension"
version = "0.1.0"
edition = "2024"
description = "Chrome/Firefox extension storage.local / storage.sync bridge"

[dependencies]
async-trait = { workspace = true }

[lints]
workspace = true
