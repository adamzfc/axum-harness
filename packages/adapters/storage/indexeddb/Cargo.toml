[package]
name = "adapter-storage-indexeddb"
version = "0.1.0"
edition = "2024"
description = "Browser-side persistent storage via IndexedDB"

[dependencies]
async-trait = { workspace = true }

[lints]
workspace = true
