tasks:
  build:
    command: 'cargo build -p application'
  test:
    command: 'cargo test -p application'
  check:
    command: 'cargo check -p application'
  lint:
    command: 'cargo clippy -p application -- -D warnings'
  format:
    command: 'cargo fmt'
